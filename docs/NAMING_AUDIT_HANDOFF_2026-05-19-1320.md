# Naming + IA fixes — handoff after 2026-05-19 13:20 audit

Codex's audit at [`NAMING_AND_COPY_AUDIT_2026-05-19-1320.md`](NAMING_AND_COPY_AUDIT_2026-05-19-1320.md) listed 9 priority fixes. This doc tracks what landed in the immediate follow-up cycle vs what's queued for future passes.

## Landed in this session

| # | Audit item | Files | Test guard |
|---|---|---|---|
| 1 | Rail decorative glyphs → `aria-hidden` so accessible name is just the group label | `playground/src/settings/SettingsShell.tsx`, `playground/src/settings/settings.css` (new `.pp-settings-rail__glyph`) | indirect via search tests |
| 3 | Setup canvas terminology: "BI tool" → "BI surface", "AI brain" → "AI assistant", "Knowledge pack" → "Domain knowledge", "Pick a BI tool" → "Pick a dashboard tool" | `playground/src/settings/groups/SetupGroup.tsx` | covered by vendor matrix tests |
| 3 | Status chip detail formatting: `powerbi` → `Power BI`, `default` → `Default profile`, `ok` → `Connected`, `strict` → `Enforced`, `(none)` → `Not configured` / `No knowledge source` | `playground/src/settings/SettingsShell.tsx` (formatters: `formatVendorName`, `formatProfileName`, `formatPackName`) | indirect |
| 4 | Footer label: "Fine-tune further" → "Continue setup" | `SetupGroup.tsx` | indirect |
| 4 | Drop "↗" arrow from leaf label: "Browse library ↗" → "Browse library" | `AiGroup.tsx`, `SettingsShell.tsx` dictionary | drift test updated |
| 9 (scope clarifier) | Rename shared-scope leaf "AI Insights" → "Response behavior" with helper text "Shared with both AI Insights and Ask Pulse" — clarifies that prompt strategy / domain guidance / metric semantics / sections are NOT Insights-only | `AiGroup.tsx`, `SettingsShell.tsx` dictionary, `AiGroup.test.tsx` updated | new test wording verifies "Shared with both AI Insights and Ask Pulse" copy |

**Tests:** 920/920 passing. TypeScript clean.

## Deferred — needs dedicated cycle(s)

| # | Audit item | Why deferred | Estimated effort |
|---|---|---|---|
| 2 | Surface switcher `BI BI Viz` duplication | Already fixed in the prior surface-UX cycle (2026-05-19 morning). Codex's audit was captured before that cycle landed — verify it's gone in the next Codex pass. | Verify only |
| 5 | Settings SubSection headings: all-caps → Title Case (`CURRENT STATE` → `Current State`, `CONNECT AND EMBED` → `Connect & Embed`, etc.) | Touches `BiGroup.tsx`, `AiGroup.tsx`, `SystemGroup.tsx`, possibly `AdvancedGroup.tsx`. Several headings, each with its own SubSection invocation. Plus the SubSection component itself may currently `text-transform: uppercase` — needs CSS + content split. | 1-2 hours |
| 6 | Humanize generated-output provenance footer (`AI-generated · Source: default · 19 min ago` → `Generated by PulsePlay · Verified with data · Source: Sales Team profile · Updated 19 min ago`) | Lives inside the Pulse-PBI compat layer (`playground/src/pulse/*`) — that surface mirrors the sibling project. Changes must respect Pulse-PBI portability (see `docs/PULSE_PORT_DETANGLING.md`). Requires touching `insightsRenderer*`, possibly `insightsProvenance*`. Display-name lookup needs to map `default` → "Sales Team profile" via profile metadata fetch. | 2-3 hours |
| 7 | Launchpad: hide raw Databricks ids; show friendly display name with id demoted to metadata | `playground/src/launchpad/LaunchpadShell.tsx` + workspace-object display logic. Needs a friendly-name fallback layer. | 2-4 hours |
| 8 | Trend arrows `▲ / ▼` with explicit text equivalents for screen readers (`increased` / `decreased`) | Already partially done in metric pills (`getDeltaPillA11y`); audit + confirm every directional glyph has an aria-hidden + textual alternative. | 1 hour audit + ~1 hour fixes |
| 9 (full IA tree) | Restructure Settings → AI into the recommended Assistant / Shared context / Response behavior / Surface-specific sub-routes | The big architectural move. Requires: (a) creating 3-4 new sub-route components, (b) moving controls between them per the audit's mapping table, (c) updating GROUP_LEAF_LABELS + drift test + scroll test + dispatcher + sub-leaf rendering, (d) preserving deep-link backwards-compat (`/settings/ai/ai-insights` → 301-style fallback to new path), (e) updating documentation. | 6-10 hours |

## Suggested sequence for the next cycle

Per Codex's "Implementation Order":

1. ✅ Surface switcher labels + accessible names (already done in surface-UX cycle)
2. ✅ Settings shell nav chips + decorative-glyph cleanup (this cycle)
3. ✅ Setup group terminology + status chip formatters (this cycle)
4. AI output provenance footer humanization (item 6 above) — high visibility win
5. Launchpad friendly-name fallback (item 7)
6. Settings SubSection Title Case sweep (item 5)
7. Trend-arrow a11y audit (item 8)
8. Full IA restructure (item 9) — separate, dedicated cycle

## Copy rules locked

Per Codex's audit § "Copy Rules To Lock":

1. ✅ Visible copy should be words, not decorative symbols → applied to rail + setup footer + status chips
2. ✅ Icons aria-hidden when decorative → applied to rail glyphs
3. ✅ Accessible names must not include decorative glyphs or duplicated words → SurfaceSwitcher already fixed; rail now too
4. ⏸ Title Case for section headings → SubSection headings still uppercase (deferred, item 5)
5. ✅ No raw enum values as primary labels → status chips now show "Power BI" not "powerbi"
6. ✅ Technical ids allowed in Advanced / Developer Tools — Launchpad pending (item 7)
7. ⏸ Honest readable AI provenance → footer still raw (deferred, item 6)
8. ✅ Same surface names everywhere → AI Insights / Ask Pulse / BI Viz consistent post-surface-UX cycle

## Tripwires preserved

- ✅ No Pulse-PBI sibling compat surface broken
- ✅ No production code touched in the audit Codex did (this cycle only adds the follow-up fixes)
- ✅ `pulseplay:*` localStorage key prefix preserved
- ✅ Proxy contract untouched
- ✅ All 920/920 baseline tests pass

## What the next session should pick up

Suggested ordering for fastest user-visible impact:

1. **Item 6** — Provenance footer (every Insights answer shows it; biggest visible per-pixel win)
2. **Item 5** — SubSection Title Case sweep (cosmetic but pervasive)
3. **Item 7** — Launchpad friendly names
4. **Item 8** — Trend-arrow a11y audit
5. **Item 9** — Full IA restructure (separate cycle; foundation already in place with sub-route infrastructure + surface registry)

## Evidence references

- Source audit: [`NAMING_AND_COPY_AUDIT_2026-05-19-1320.md`](NAMING_AND_COPY_AUDIT_2026-05-19-1320.md)
- Codex evidence folder: `docs/evidence/naming-audit-2026-05-19-1320/`
- Previous surface-UX cycle: [`SURFACE_COMPANION_HANDOFF_2026-05-19.md`](SURFACE_COMPANION_HANDOFF_2026-05-19.md)
