# Exact DDL & generation-source inventory

Generated by `scripts/synthetic_poc` (contract-driven). Every table is Delta, physical (non-measure) columns only. All values synthetic.

### `workspace.databrickspractice.tbl_pp_syn_dm_countries`  (dimension, 20 rows, 7 cols, 0 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_dm_countries (
  `country_id` string,
  `country_text` string,
  `ou` string,
  `language` string,
  `latitude` double,
  `longitude` double,
  `id` bigint
) USING DELTA
COMMENT 'SYNTHETIC POC clone of vw_ltm_sc_dm_countries (genie-01f130be); 1% fact scale; no real data'
```

### `workspace.databrickspractice.tbl_pp_syn_dm_plants`  (dimension, 602 rows, 10 cols, 0 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_dm_plants (
  `plant_id` string,
  `descr` string,
  `location` string,
  `country_id` string,
  `region` string,
  `ou` string,
  `loc_type` string,
  `lat` double,
  `lon` double,
  `pallet_cap` double
) USING DELTA
COMMENT 'SYNTHETIC POC clone of vw_ltm_sc_dm_plants (genie-01f130be); 1% fact scale; no real data'
```

### `workspace.databrickspractice.tbl_pp_syn_dm_sales_channel`  (dimension, 11 rows, 2 cols, 0 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_dm_sales_channel (
  `sales_channel_text` string,
  `country_channel` string
) USING DELTA
COMMENT 'SYNTHETIC POC clone of vw_ltm_sc_dm_sales_channel (genie-01f130be); 1% fact scale; no real data'
```

### `workspace.databrickspractice.tbl_pp_syn_fct_ofr`  (fact, 182,921 rows, 28 cols, 25 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_fct_ofr (
  `period_type` string,
  `country_id` string,
  `country_text` string,
  `ou` string,
  `year` int,
  `month` int,
  `week` int,
  `quarter` int,
  `quarter_name` string,
  `month_name` string,
  `week_label` string,
  `month_order` int,
  `month_year` timestamp,
  `material_id` string,
  `sales_channel_id` string,
  `sales_channel_text` string,
  `plant_id` string,
  `plant_name` string,
  `plant_descr` string,
  `plant_location` string,
  `plant_region` string,
  `plant_loc_type` string,
  `sales_office` string,
  `country_channel` string,
  `reject_id` string,
  `reject_text` string,
  `impact_fr` string,
  `month_text` string
) USING DELTA
COMMENT 'SYNTHETIC POC clone of mtr_vw_ltm_sc_fct_ofr (genie-01f130be); 1% fact scale; no real data'
```

### `workspace.databrickspractice.tbl_pp_syn_fct_operations`  (fact, 846 rows, 18 cols, 91 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_fct_operations (
  `source_kpi` string,
  `period_type` string,
  `country_id` string,
  `country_text` string,
  `ou` string,
  `year` int,
  `month` int,
  `quarter` int,
  `quarter_name` string,
  `month_name` string,
  `month_order` int,
  `month_year` timestamp,
  `plant_id` string,
  `plant_descr` string,
  `plant_location` string,
  `plant_region` string,
  `location_name` string,
  `line_of_business` string
) USING DELTA
COMMENT 'SYNTHETIC POC clone of mtr_vw_ltm_sc_fct_operations (genie-01f130be); 1% fact scale; no real data'
```

### `workspace.databrickspractice.tbl_pp_syn_fct_performance`  (fact, 177 rows, 14 cols, 71 measures excluded)
```sql
CREATE TABLE workspace.databrickspractice.tbl_pp_syn_fct_performance (
  `source_kpi` string,
  `period_type` string,
  `country_id` string,
  `country_text` string,
  `ou` string,
  `year` int,
  `month` int,
  `quarter` int,
  `quarter_name` string,
  `month_name` string,
  `month_order` int,
  `month_year` timestamp,
  `job_type` string,
  `week_date` timestamp
) USING DELTA
COMMENT 'SYNTHETIC POC clone of mtr_vw_ltm_sc_fct_performance (genie-01f130be); 1% fact scale; no real data'
```

### Metric views — BLOCKED (not created)
Measure aggregation formulas are redacted in the contract package (`grandTotalAggregate` + comment only; no `measures:` YAML). Reproducing them would require inventing redacted logic, so per policy they are left BLOCKED:
- `workspace.databrickspractice.mtr_pp_syn_ltm_sc_fct_ofr`
- `workspace.databrickspractice.mtr_pp_syn_ltm_sc_fct_operations`
- `workspace.databrickspractice.mtr_pp_syn_ltm_sc_fct_performance`