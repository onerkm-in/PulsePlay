---
name: PulsePlay current state
description: As of 2026-05-21 - G2 pure visualization pipeline and G2.5 Databricks source-ref contract are shipped, PX1 unified proxy client contract is shipped, F5/F5.1 layout state is automated-green, G0/G1 native adapter architecture plus renderer-only skeleton are shipped, HOSTING_OPTIONS.md covers deployment hosting choices, SETTINGS_AUTHOR_VIEWER_UX_SCAN.md captures the Settings author/viewer UX structure pass, ADR-0010 locks the ecosystem artifact strategy, PULSE_SYNC.md tracks Pulse PBI + desktop EXE cascade impact, the proxy has 10 backend paths, latest proxy validation is 1077/1077, latest playground validation is 1273/1273 plus lint/build, and only partial headless native setup smoke has run
type: project
originSessionId: current
---

**Branch:** `codex/f5-g0-native-layout-2026-05-21` off `publish/local-main-2026-05-20` (the effective working trunk; local `main` is tracking it for now — see the cycle-11 finding below about why `origin/main` shares no common ancestor and must not be force-pushed).

**Latest shipped implementation slice - 2026-05-21:** G2 pure visualization pipeline, commit `9ff892a`. Added [`playground/src/visualization/aiResultEnvelope.ts`](../../playground/src/visualization/aiResultEnvelope.ts), [`resultToVizIntent.ts`](../../playground/src/visualization/resultToVizIntent.ts), [`chartAutoPick.ts`](../../playground/src/visualization/chartAutoPick.ts), and [`chartSpecValidation.ts`](../../playground/src/visualization/chartSpecValidation.ts). The modules are pure TypeScript: no DOM, React, fetch, localStorage, vendor SDK, proxy client, or renderer imports. `AIResultEnvelope.sourceRef` is already narrowed to `DatabricksSourceRef`; `governance` stays opaque until G3. Pulse-ported chart helpers now import shared chart-pick policy instead of duplicating it, so downstream Pulse behavior has been generalized upstream. Workbench chart tabs validate specs before compiling to ECharts and reject external data URLs. Validation: focused G2 + adjacent chart/workbench tests **145/145**, post-commit focused visualization tests **76/76**, full playground **1273/1273**, lint clean, build clean.

**Previous shipped implementation slice - 2026-05-21:** G2.5 Databricks source-ref contract. Added [`playground/src/visualization/sourceRef.ts`](../../playground/src/visualization/sourceRef.ts) — pure TypeScript discriminated union `DatabricksSourceRef` over five kinds (`genie-space`, `metric-view`, `uc-function`, `view`, `table`), with `DATABRICKS_SOURCE_REF_KINDS` frozen list, per-kind type guards, `isDatabricksSourceRef` trust-boundary validator, and `sourceRefDisplayLabel` formatter. Every variant requires `displayName` and `governance: { requiresAttestation: true }`; the `table` variant additionally carries the literal `warning: "raw-table-bypasses-curated-views"` at the type level so UI consumers always render the warning. `uc-function` supports optional typed parameters for parameterized governed queries — proxy binds server-side, browser never concatenates SQL. Pure module: no DOM, fetch, React, localStorage, or SDK imports — Pulse PBI copy-port safe (Tier 2 in PULSE_SYNC). Validation: focused sourceRef tests **55/55**, full playground **1252/1252**, lint clean, build clean.

**Previous shipped implementation slice - 2026-05-21:** PX1 unified proxy client contract shipped in `22db943`. New [`proxy/lib/pulseClientContext.js`](../../proxy/lib/pulseClientContext.js) normalizes `X-Pulse-Client` to `pulseplay`, `pulse-pbi`, `pulseplay-desktop`, or `unknown`, sanitizes `X-Pulse-Client-Version`, and accepts `X-Pulse-Request-Id` as a Pulse-named correlation fallback after `X-Request-Id`. [`proxy/server.js`](../../proxy/server.js) allows/exposes the PX1 headers in CORS, echoes normalized `X-Pulse-Client` plus both request-id headers, stamps audit logs with `clientApp` and optional `clientVersion`, adds client identity to `/health`, and adds auth-free `GET /clients/compatibility`. Validation: focused PX1/server tests **149/149**, full proxy **1077/1077**, `node --check proxy/server.js` PASS. This is shared proxy-contract work only; G3 governance attestation is still queued.

**Latest shipped docs slice - 2026-05-21:** [PULSE_SYNC.md](../PULSE_SYNC.md) now records the G2 visualization pipeline modules and contracts at version `0.1`, synced to `9ff892a`, with Pulse PBI copy-port queued and desktop EXE inheriting through the future app bundle. The same doc remains the ecosystem cascade ledger: every meaningful PR must state Pulse PBI impact and Desktop EXE impact in the PR template; non-`N/A` answers update the ledger. It classifies shared proxy contracts, portable modules, host-specific UI, desktop packaging, and docs-only changes, adds a desktop cascade checklist for bundled-proxy compatibility, encrypted local persistence, browser private-mode behavior, and future `PulsePlayData/` implications, and explicitly preserves Pulse PBI's Power BI iframe/custom-visual sandbox boundary. PX1 is recorded there as proxy-client-contract `0.1` synced to `22db943`.

**Previous docs slice - 2026-05-21:** [ADR-0010 - PulsePlay Ecosystem Artifact Strategy](../adr/0010-artifact-strategy.md) locks the artifact posture. PulsePlay is the umbrella ecosystem with one repo/download and multiple outputs: web, optional Pulse PBI `.pbiviz`, optional desktop EXE recon tool, and hosted bundles. Target layout is `enablers/pulse-pbi/` and `enablers/desktop/` so one checkout contains all enablers while preserving build/runtime isolation. The proxy contract is unified across PulsePlay/Pulse PBI/future desktop (`X-Pulse-Client` direction queued as PX1), but deployment topology can vary. Desktop is explicitly a recon tool for authors/analysts/DPMs; Tauri is the preferred DX1 shell; the EXE must bundle the app server/proxy so users do not install Node/npm or start a separate server; desktop builds must not ship a lite proxy; and `apps/`/`packages/` refactors remain deferred. Runtime PBIVIZ/EXE work stays after G3/PB0 unless explicitly redirected.

**Previous UX docs slice - 2026-05-21:** [SETTINGS_AUTHOR_VIEWER_UX_SCAN.md](../SETTINGS_AUTHOR_VIEWER_UX_SCAN.md) captures the Settings/Setup/viewer UX structuring scan. Verdict: keep the current Settings shell and add a role/scope layer rather than rewriting. The scan separates Viewer, Author, Admin/Deployer, and Support/Developer needs; flags P1s around truthful Save bar semantics, fail-closed UI, mobile Settings navigation, and a pure `AuthoringStateSnapshot`; and queues UX1-UX4 for Settings truth, Author Setup Home, trust/mode cards, and Figma/Canva handoff. It also records the unified-proxy stance for Pulse PBI + PulsePlay: one proxy codebase/API/governance/audit/result-envelope contract, with separate deployments allowed only for local debugging or isolation, not as a connector-logic fork. It was informed by a headless DOM pass against `http://127.0.0.1:5173` with proxy down, but does not count as browser UX certification.

**Previous deployment docs slice - 2026-05-21:** [HOSTING_OPTIONS.md](../HOSTING_OPTIONS.md) is now the deployment hosting decision guide. It covers Databricks Apps, Azure Static Web Apps, Azure Storage + Front Door, Azure Container Apps, Azure App Service, AKS, enterprise PaaS/OpenShift, VM/IIS/Nginx, serverless rewrite, and the rejected frontend-only path. Recommendation: Databricks App for fastest Databricks-native pilot; Azure split-host static frontend + Container Apps/App Service proxy for the enterprise default; single App Service for simplest fallback; AKS/VM only when already mandated. It locks the current Databricks-first assumption: Azure Databricks + ADLS + Unity Catalog + SQL Warehouse + Metric Views/UC Views/Genie Spaces are the core data/AI plane, SQL stays behind the proxy/data layer, and the native adapter remains renderer-only. Validation was docs-only `git diff --check` with LF-to-CRLF warnings only.

**Previous shipped implementation slice - 2026-05-21:** F5/F5.1 layout state contract + G0/G1 native adapter foundation. G0 added `docs/feature_native_adapter.md`, ADR-0009, `docs/PULSE_SYNC.md`, PR template sibling-sync guidance, and feature memories. F5/F5.1 added persisted requested surface intent, effective surface resolution, `?surface=`, `data-active-surface` / `data-requested-surface`, fallback reason telemetry, and disabled-but-visible surface switcher states. G1 (`48d966a`) added `bi-adapters/native/` with a loadable renderer-only `BIAdapter` skeleton, capability declarations, renderer command vocabulary, forbidden command rejection, registry wiring, no-embed readiness handling, and import-boundary tests. G1 audit patch `931f62f` made `NativeBIAdapter.emit()` deterministic under listener reentrancy and added one test. Validation: focused native/registry/readiness/vendor matrix **77/77**, full playground **1197/1197**, playground lint clean, playground build clean with existing BI-adapter dynamic-import warnings. In-app Browser plugin still fails with the kernel-asset write blocker; headless Playwright fallback verified preselected native setup state (native note visible, embed textarea hidden) with proxy down. Do not call F5/G1 fully UX-verified yet.

**Latest Settings validation/fix - 2026-05-20:** Codex ran a multi-agent focused Settings audit and fixed the user-suggested Setup -> AI lane. Quick Setup now uses live proxy profiles/packs when the allowlist is unconfigured, `Test selected profile` parses the proxy's direct array response shape, and the AI docs link is connector-aware. Evidence: [`docs/evidence/settings-regression-2026-05-20-codex`](../evidence/settings-regression-2026-05-20-codex). Validation: playground lint PASS, targeted Setup/AI tests **32/32**, full Settings test slice **155/155**. Remaining P1 Settings backlog is in [`docs/AGENT_SYNC.md`](../AGENT_SYNC.md) and [`docs/AGENDA.md`](../AGENDA.md): mobile nav, Save-bar Discard state restore, redaction, reset coverage, Power BI secure-host validation, and fail-closed embed config behavior.

**Latest docs consolidation - 2026-05-20:** Canonical navigation now starts at [`docs/README.md`](../README.md). It separates active docs from archive/evidence files, records the current 10-backend/2116-test state, and defines the next link-preserving merge plan. Active docs were refreshed to stop repeating old backend totals, old proxy/playground test counts, and the superseded ADR-0003 filename. Dated reports and `AGENT_SYNC.md` remain evidence, not first-read material.

**Latest Codex UI regression — 2026-05-20:** Visible in-app browser pass documented in [`docs/CODEX_UI_REGRESSION_RESULTS_2026-05-20.md`](../CODEX_UI_REGRESSION_RESULTS_2026-05-20.md), evidence in [`docs/evidence/ui-regression-2026-05-20-codex`](../evidence/ui-regression-2026-05-20-codex). No product code changed. Proxy had to be restarted to pick up the cycle-15.5 `/powerbi/qna/embed-token` route; after restart the route surfaces the expected config error (`No Power BI semantic-model profile configured`) instead of 404. Confirmed open blockers: Setup AI profile select still filters to empty because allowlist is `configured:false` with `aiProfiles:[]` while `/assistant/profiles` returns 3 profiles; Setup still hardcodes `Databricks docs`; live Ask Pulse submission is blocked by local Node CA trust (`unable to verify the first certificate`); HelpTip still logs a React setState-in-render console error. Root/Settings/Knowledge/Workbench routes render, current UI says `Dashboard` where older UAT docs expected `BI Viz`, and 390x844 root/setup checks showed no horizontal overflow.

**Most recent session — 2026-05-20 cycle 11 → 15.5 (six PRs merged):**

- **Audit close-out (cycle 11, PR #1).** SigV4 duplicate signer removed in `proxy/server.js#bedrockRetrieveAndGenerate` (now delegates to `proxy/lib/bedrock.js`). ADR-0003 body rewritten with the full 350 → 800 → 1500 → 2000 ms tuning history. CLAUDE.md stale citations fixed. New `.github/workflows/test.yml` CI workflow (proxy jest + playground lint/vitest/build on PR + push to main/publish/**). HANDOVER cycle 11 entry. **Pre-flight finding (still load-bearing):** `origin/publish/local-main-2026-05-20` and `origin/main` share NO common git ancestor; 496 files differ; do NOT push publish → main without a strategic reconciliation cycle.
- **DwD purge + probe-once Genie (cycle 12, PR #1).** Full repo sweep: `dwd-supervisor-agent` → `pulseplay-supervisor-agent`, cache prefixes bumped (`dwd-ai-insights:v6:` → `pulseplay-ai-insights:v7:` per ADR-0005), localStorage keys, window debug key, SQL aliases, default DB table fixture (configurable per profile so deployers with the old table name still work), legacy session-state fallback removed entirely. `grep dwd` across tracked files: 0 matches. **Probe-once for Genie:** App.tsx prewarms `getDiscoverySnapshot()` when (profile, pack) becomes known; Pulse `genie.ts` attaches `discoveryContext` (a 20-KPI / 12-frame summary of the cached snapshot) on every `/conversations/start` + follow-up POST; proxy injects `[Discovery Context]` block above `[Pack Context]` above `[User Question]`. Audit emits `discovery-context-inject`.
- **Author-selectable latency levers (cycle 13, PR #2).** Single `pulseplay:performance-levers` localStorage bag with 4 knobs: `revealCadence` (instant / fast / balanced / full), `discoveryPrewarmEnabled` (bool), `insightsCacheTtlMinutes` (1..180, default 30), `maxValidationRetries` (0..3, default 1). Settings → Advanced → Performance UI panel with 2×2 cadence picker + checkbox + 2 sliders + Reset-to-defaults. Server-side: `maybeValidateGeniePollResponse` honors client `maxValidationRetries` (clamped 0..3) over the `GENIE_POLL_VALIDATE_RETRIES` env default. Logic extracted to `proxy/lib/validationRetryBudget.js` for unit testability. Mid-session event broadcast (`pulseplay:performance-levers-change`).
- **Cross-backend probe symmetry (cycle 14, PR #3).** Cycle 12's `discoveryContext` injection extended from Genie-only to all 7 backend routes: FM single (`/foundation/section`), FM staged (`/assistant/conversations/start-sectioned`), OpenAI Chat, Bedrock direct (system prompt), Bedrock RAG (user-message header), Supervisor (user-message header), Genie. Two shared composers in `proxy/lib/discoveryPromptInjector.js`: `composeUserMessageWithContext` + `composeSystemPromptWithContext`. Probe failure visibility: new `playground/src/lib/probeStatusStore.ts` pub/sub with phases `idle | probing | ready | failed` + window event `pulseplay:probe-status`. Replaces cycle-12's silent `.catch(() => {})`.
- **Power BI semantic-model AI brain — no-LLM, deterministic (cycle 15, PR #4).** Tenth backend path. Pipeline: AAD SP token → DAX `INFO.*` probe → keyword matcher → DAX template → `executeQueries` → Markdown. Four templates: `top-n`, `aggregate-by`, `trend`, `total`. Strict slot-value sanitiser closes DAX injection vectors. Matcher: longest-match measure, table-name fallback, plural-aware column matching ("categories" → `Category`), word-form numbers ("top five" → 5), time detection by name + by type. Every response emits `mode: "powerbi-deterministic", llmCallCount: 0` in body + audit. Probe adapter in `proxy/lib/connectorProbe.js` reads `INFO.MEASURES` / `INFO.TABLES` / `INFO.COLUMNS` via DAX so PBI measures become `declaredKpis` aligned with pack KPI matching the same way Genie's are.
- **Power BI Q&A embed surface (cycle 15.5, PR #5).** Second answer surface for the same PBI connector. `proxy/lib/powerbiDatasetClient.js` gains `generateQnAEmbedToken` (View accessLevel, optional RLS identities). New `POST /powerbi/qna/embed-token` route. Client: `playground/src/lib/powerbiQnAClient.ts` + `playground/src/components/PowerBiQnA.tsx` (lazy-loads `powerbi-client` SDK, embeds with `QnaMode.Interactive`, auto-refreshes token 5 min before expiry). Standalone route at `/powerbi/qna` with full-viewport shell + back-to-app button. **Microsoft's NLP runs in Microsoft's tenant** — PulsePlay only mints the embed token; audit emits `powerbi-qna-token-minted` with `llmCallCount: 0`.

**Net test delta across the session:** proxy **910 → 1013** (+103), playground **1063 → 1103** (+40). Lint + build clean throughout. Six PRs, no rollbacks.

**Architectural decision locked (2026-05-20):** **connector plugin system.** Refactor `proxy/server.js` dispatch into a `proxy/connectors/` directory of drop-in/drop-out modules. Contract: `{ id, displayName, matchProfile, probe, register, unregister }`. Host API surface defined (only what 2+ connectors need). Phased rollout — A (scaffolding) → B (one pilot, recommend `bedrock`) → C (rest). Full spec lives in `docs/AGENT_SYNC.md` `[DECISION]` block + `docs/AGENDA.md` "Next up".

**Previous session work (carried for context):**

- **Staged SSE `renderId` envelope after Genie message probe (2026-05-20).** User supplied [`docs/findingProbeIssue.md`](../findingProbeIssue.md) and a Databricks HAR. The markdown finding is valid: Genie REST is one-message-per-POST and cannot append multiple section calls into one upstream `message_id`. The HAR was checked and is not API-level proof (it contains Databricks UI assets, `popproxy/health`, `data-rooms/.../value-index`, and telemetry; no `/api/2.0/genie/*` calls). Shipped Path A:
  - [`proxy/lib/sectionedOrchestrator.js`](../../proxy/lib/sectionedOrchestrator.js) now generates/preserves one `renderId` per orchestration and stamps it on every event; `createRenderId()` / `resolveRenderId()` are exported for route-level use and tests.
  - [`proxy/server.js`](../../proxy/server.js) accepts optional `renderId` on `/assistant/conversations/start-sectioned`, passes it through, and includes it on terminal `orchestrator-failed` frames.
  - [`playground/src/hooks/useSectionedStream.ts`](../../playground/src/hooks/useSectionedStream.ts) exposes `renderId`, captures it from SSE, and reuses it when `regenerate(sectionId)` posts a selective rerun.
  - [`playground/src/styles.css`](../../playground/src/styles.css) adds the missing `SectionedAnswer` styling. Visual assessment found the primitive had lifecycle classes but no CSS, so it would have looked like a plain ordered list despite passing tests.
  - [`AGENTS.md`](../../AGENTS.md), [`CLAUDE.md`](../../CLAUDE.md), [`docs/STAGED_RENDERING.md`](../STAGED_RENDERING.md), and [`proxy/tests/genieSqlSections.test.js`](../../proxy/tests/genieSqlSections.test.js) now carry the immutable-Genie-message tripwire: N Genie `message_id`s under one `conversation_id`, one UI assistant envelope keyed by PulsePlay `renderId`.
  - **Validation:** focused proxy sectioned tests **59/59**; focused hook/parser + visual component tests **26/26** after CSS; full proxy `npm test` **910/910**; playground `npm run lint` clean; full playground `npx vitest run` **1063/1063** after CSS; playground `npm run build` green with the existing BI-adapter chunking warnings.
  - **Browser visual smoke:** local proxy + Vite were started; app shell screenshot and actual `SectionedAnswer.tsx` harness screenshot saved under [`docs/evidence/renderid-ui-smoke-2026-05-20`](../evidence/renderid-ui-smoke-2026-05-20). Harness check: 5 rendered items, 2 completed, 1 streaming, 1 pending, 1 failed; harness console clean.
  - **Honest deferrals / state:** this does not and cannot make Genie share one upstream `message_id`; it prepares the grouping contract for the future live Genie staged route. `SectionedAnswer` is still primitive-level smoke-tested, not live AISidebar-integrated. Pre-existing unrelated working-tree changes in `playground/package*.json`, `playground/src/App.tsx`, and deleted `DayCycleBubble.tsx` were left alone.

- **Phase E — client-side staged reveal for Genie single-shot answers (2026-05-20).** Three-commit slice on `main` + a follow-up render-gate test commit, all green. Closes the UX gap left by Phase D: Phase D streams when the LLM supports a sectioned SSE protocol (FM only today); Phase E cosmetically paces the SINGLE-SHOT Genie markdown briefing on the client so the user sees the same staged-reveal feel without re-querying Genie.
  - `b3412c8` E.1 [`playground/src/pulse/state/stagedReveal.ts`](../../playground/src/pulse/state/stagedReveal.ts) pure schedule primitives + **17 vitest**. Transport-agnostic; exports `DEFAULT_REVEAL_SCHEDULE` (HEADLINE@0 / KPI SNAPSHOT+TRENDS@10s / RISKS+RECOMMENDED ACTIONS@20s / OPPORTUNITIES@30s), `STAGE_GAP_MAX_MS=60000`, `validateSchedule`, `computeRevealState`, `nextRevealTickMs`. Stages whose sections aren't present in the parsed briefing are pruned so the strip never shows phantom future pills. Sections not named in any stage reveal unconditionally (safety fallback for custom-Adjust briefings).
  - `39f7e43` E.2 [`playground/src/pulse/visual.tsx`](../../playground/src/pulse/visual.tsx) wiring. `InsightsRenderOptions` gained `revealedSectionTitles?: Set<string> | null`; inside `renderInsightsSections` held-back sections render as `InsightsSectionPlaceholder` (existing skeleton chrome with `gn-insights-section--placeholder` modifier + `aria-busy=true`). App-level: `parsedSectionTitlesForReveal` memo re-parses `#` headings same way as the renderer; `revealState` memo (frozen state at content-arrival timestamp + `revealTick`); `setTimeout` scheduler ticks via `setRevealTick`; `prefers-reduced-motion: reduce` → null reveal state (instant render). Stage progression strip inserted above the section grid: `role="status" aria-live="polite"`, ✓/●/○ pills with `data-stage-index` + `data-stage-status`, inline-styled green/purple/grey palette, "· next in Ns" countdown on the current stage. New Settings toggle [`insightsStagedRevealEnabled`](../../playground/src/pulse/settings.ts) (default `true`) wired through to [`PulseAiVisualSettings`](../../playground/src/settings/pulseVisualSettingsStore.ts).
  - `f8d03c4` E.3 [`docs/HANDOVER.md`](../HANDOVER.md) cycle 9 entry.
  - E.4 (this commit) [`playground/src/pulse/__tests__/insightsStagedReveal.test.tsx`](../../playground/src/pulse/__tests__/insightsStagedReveal.test.tsx) — 4 focused render-gate tests pin: back-compat path (undefined `revealedSectionTitles` → all sections render live, no `--placeholder` modifier); stage-0 reveal (only HEADLINE in the set → exactly 4 `--placeholder` modifier hits); stage-1 reveal expansion (3 in the set → exactly 2 placeholders); fully-revealed set behaves identically to back-compat null.
  - **Validation:** TypeScript clean, `npm run lint` clean, full playground `npx vitest run` **1063/1063** (was 1042 → +17 schedule + +4 render-gate). Proxy untouched (787/787 still applies from cycle 7).
  - **Honest deferrals (carried forward):** Phase D + Phase E remain disjoint paths: D is real streaming for FM, E is cosmetic pacing for Genie single-shot. Wiring D's SSE event vocabulary into the AISidebar for Genie + token-streaming for FM are still queued cycles. No live browser smoke yet (proxy terminal had exited code 1 — unit/integration coverage only this cycle).
  - **Tripwires preserved:** Pulse-PBI compat surface untouched semantically (additive option on `renderInsightsSections`; the `revealedSectionTitles` option is opt-in via App-level state, sibling project doesn't pass it); no Genie sandbox widening; no validator changes; no credentials in browser bundle; no schedule longer than `STAGE_GAP_MAX_MS=60000` (validateSchedule guards); reduced-motion honoured by default.

- **Phase D staged "1-then-3" rendering — full slice (2026-05-20).** Four-commit beast-mode delivery on `main`, all green: orchestrator + SSE endpoint + UI primitive + selective re-run convenience method. User-amended schedule encoded in `DEFAULT_SCHEDULE`: head of 2 with 2000 ms spread between section 1 and section 2 (HEADLINE then KPI), then 2-each batches (TRENDS+RISKS, then RECOMMENDED_ACTIONS+OPPORTUNITIES).
  - `c28da22` D.1 `proxy/lib/sectionedOrchestrator.js` + 37 unit tests — transport-agnostic + LLM-agnostic; caller injects `runProbe` + `runSection` thunks; `buildDefaultSchedule`/`validateSchedule` helpers; SPREAD_MAX_MS=30s; HEADLINE result threaded to downstream stages; per-section error isolation; AbortSignal support; `regenerateOnly` + `probeCache` + `headlineCache` selective re-run.
  - `8faf690` D.2 `POST /assistant/conversations/start-sectioned` (FM-only for v1) + 11 SSE integration tests. Validation order: profile → userPrompt → sections-or-schedule → schedule shape (all 400 JSON BEFORE opening SSE). Two debugging gotchas locked: supertest doesn't auto-buffer `text/event-stream` (need `.buffer(true).parse(...)` and read `res.body`); `req.on('close')` fires too early in supertest (use `res.on('close')`).
  - `358679b` D.3 [`playground/src/components/SectionedAnswer.tsx`](../../playground/src/components/SectionedAnswer.tsx) pure render component (12 tests) + [`playground/src/hooks/useSectionedStream.ts`](../../playground/src/hooks/useSectionedStream.ts) hook with tested `parseSseChunkBuffer` (11 tests). Lifecycles: pending (skeleton + `aria-busy=true`) → streaming (`Generating…` + pulse) → completed (body + `↻ Regenerate` + meta) → failed (`role=alert` + `↻ Retry`). Hook consumes SSE via `fetch` + `ReadableStream` (EventSource is GET-only). `renderBody` override per section type.
  - `9c54f10` D.4 `hook.regenerate(sectionId)` convenience method + 3 tests. Auto-captures `probe-completed.rows` and HEADLINE body into refs during the first stream; remembers the full `start()` payload; `regenerate(id)` re-issues the SAME base payload pinned to `regenerateOnly:[id]` with caches attached. Peer sections stay `completed`; named section blanks to `pending` then lands `completed` with new body. Multiple successive regenerates do NOT clobber the cached base.
  - **Validation:** proxy 851→**899** (+48: 37+11); playground 1016→**1042** (+26: 12 SectionedAnswer + 11 hook/parser D.3 + 3 hook regenerate D.4). Playground `npm run lint` clean.
  - **Honest deferrals (carried forward):** FM-only — Genie path (probe SQL + follow-up-message-per-section pattern) queued and already documented in [`docs/STAGED_RENDERING.md`](../STAGED_RENDERING.md); no real probe yet (v1 `runSection` works directly from `userPrompt`); no token-streaming (event vocabulary supports `section-token` but it's not emitted or consumed today); AISidebar / live surface wiring is the follow-up cycle; no live browser smoke this cycle (exhaustive unit + integration coverage only).
  - **Tripwires preserved:** no `playground/src/pulse/*` touched (Pulse-PBI compat untouched); no Genie sandbox widening; no validator authority changes; no credentials in browser bundle (FM call stays server-side); vendor-axis untouched (this is the AI-render plane).

- **Power BI + Databricks intensive test pass + cross-plugin parity (2026-05-20).** Hardened the plugin-architecture layer with vendor-agnostic discipline (no Power-BI privilege) — every BI tool, Power BI included, is just one plugin behind the shared `BIAdapter` contract. Five commits, all green:
  - `a86241c` `test(databricks-genie)` — 7 → **41 tests** ([bi-adapters/databricks-genie/__tests__/index.test.ts](../../bi-adapters/databricks-genie/__tests__/index.test.ts)): URL parsing (URI-encode spaceId, embedPath leading-slash normalize, trailing-slash workspaceUrl trim, iframe src extraction with single-quote, case-insensitive SRC=, `&amp;` decode), partial structured-config error paths, vendor identity, **default sandbox lock-in + negative deny-list** (no `allow-top-navigation`/`modals`/`storage-access`/`orientation-lock`/`pointer-lock`), `cfg.sandbox` override, `allowedOrigins` L2 gate allow + deny, title defaults, destroy idempotency, remount after destroy. Locked the current contract that whitespace-only `url` short-circuits the `cfg.url || cfg.iframe` OR (does NOT fall back to iframe — throws EMBED_FAILED) so the honest behavior is auditable.
  - `7d16ed3` `test(databricks-aibi)` — 21 → **33 tests** ([bi-adapters/databricks-aibi/__tests__/index.test.ts](../../bi-adapters/databricks-aibi/__tests__/index.test.ts)): SDK path via top-level `vi.mock("@databricks/aibi-client", () => ({...}))` factory (Vitest intercepts the dynamic `await import()` even when the module isn't installed); `hideDatabricksLogo` threading; `token + accessToken` alias; `workspaceUrl` fallback when `instanceUrl` absent; SDK-absent + no fallback URL → EMBED_FAILED; iframe fallback path; destroy calls both `.destroy()` and `.dispose()` defensively, swallows errors, removes sdkContainer.
  - `9f95602` `test(powerbi)` — 49 → **67 tests** ([bi-adapters/powerbi/__tests__/index.test.ts](../../bi-adapters/powerbi/__tests__/index.test.ts)): mount config translation (`tokenType` default `Embed=1`, `Aad=0`; `permissions` `Edit=All=7`, `View=Read=0`; mount-twice resets prior state); event bridging (`loaded` subscription registers BOTH `loaded` AND `rendered` PBI events; `data-refreshed` bridges `dataRefreshed`; `error` bridges PBI error — payload exposes `pbiEventName + raw` not flattened `.message`); secure-iframe sandbox defaults + `cfg.sandbox` override + URL gating (non-reportEmbed rejected, non-https rejected); `getDeveloperSnapshot` error branches (each of `getPages`/`getActivePage`/`getFilters` throw recorded in `errors[]`; all-three-fail accumulation; note contains "live powerbi-client").
  - `4c1b1a1` `test(biPanel): cross-plugin parity` — NEW [playground/src/biPanel/__tests__/registry.parity.test.ts](../../playground/src/biPanel/__tests__/registry.parity.test.ts) (**14 tests**): exact registered set of 7 plugins; `VendorInfo` shape contract; only `generic-iframe` ships `configured:true`; unknown vendor throws actionable error enumerating known IDs; every plugin loads through the same surface (vendor/displayName match registry, `mount`/`destroy`/`on`/`send`/`capabilities` all functions, capabilities returns full shape); Power BI advertises filters + nav vs `generic-iframe` does not (asymmetry is INTENTIONAL vendor-specific, not host privilege); `on()` returns unsubscribe; `send()` pre-mount throws `NOT_MOUNTED` for every plugin (vendor-neutral host invariant); `destroy()` pre-mount no-op; Vite lazy-load posture (factory not singleton, async-only signature).
  - `7c30f1c` `test(databricks-capabilityRegistry)` — 5 → **14 tests** ([proxy/tests/databricksCapabilityRegistry.test.js](../../proxy/tests/databricksCapabilityRegistry.test.js)): `forceRefresh` bypasses valid cache; `reset()` clears cache; **single-flight concurrency** coalesces 5 parallel calls into ONE probe burst (6 fetches not 30); `forceRefresh` does NOT join an in-flight probe (kicks its own burst); degraded inputs (missing profile → all-error snapshot with "No profile resolved"; missing `databricksRequest` → "databricksRequest function not supplied"); `jobs` probe `ready=true` even at count=0 (admin endpoint existence is the signal); vector-search alternate `vector_search_endpoints` key; 500-class probe errors trimmed to ≤240 chars.
  - `8d8a224` `test(databricks-enablement)` — NEW [proxy/tests/databricksEnablement.unit.test.js](../../proxy/tests/databricksEnablement.unit.test.js) (**55 tests**): every normalizer walked against alternate REST key shapes (snake_case vs camelCase, `dashboard_id`/`dashboardId`/`id`, `embed_url`/`embedUrl`/`url`, `endpoint_name`, `cluster_size`, `display_name`/`name`/`title`); `normalizeMetricView` fullName from catalog.schema.name; `isMetricView` case-insensitive; URI-encoding in workspace URLs; `buildLaunchpadPayload` defaults; `sanitizeVectorSearchQuery` clamps `[1, 50]`, NaN→5 fallback, floors decimals, snake_case alias, `query`/`text` aliases, fallback indexName, columns trim/dedup/cap-50, omits empty, filters JSON-stringify, reranker passthrough, non-object filter/reranker ignored.
  - **Tripwires honored:** PulsePlay vendor-neutrality preserved (no Power-BI-special framing); brutal-honest where the current contract surprised us (whitespace-only url in Genie; PBI error payload is `pbiEventName + raw`, not flattened `.message`) — tests lock the actual behavior, not the wish; no Pulse-PBI compat broken; no Genie sandbox widening (the new tests assert the narrow defaults stay narrow); no validator changes; no proxy production code touched.
  - **Validation:** Full proxy `npx jest` **851/851** (was 787 → +64); full playground `npx vitest run` **1016/1016** (was 952 → +64). All 5 commits stacked on `main`.
- **Claude guide for PulsePlay potential + performance recovery (2026-05-19).** Added [`docs/CLAUDE_PULSEPLAY_POTENTIAL_PERFORMANCE_GUIDE_2026-05-19.md`](../CLAUDE_PULSEPLAY_POTENTIAL_PERFORMANCE_GUIDE_2026-05-19.md) as a Claude-ready handoff for Rajesh's "performance is horrible" feedback. The guide frames PulsePlay's real strategic advantage as a unified multi-BI / multi-AI orchestration layer, but makes latency the non-negotiable blocker. It explicitly says `7c6d84e` concurrency-2 is not enough unless live timings prove otherwise, sets concrete first-useful-output targets, and lays out measurement, first-output, stage-fusion/two-pass, progressive-rendering, and warm-cache phases with UAT questions and acceptance criteria.
- **AI Insights pipeline: concurrency-2 with stage-0 head-start (2026-05-19).** First concrete latency lever for AI Insights per Rajesh's "process two sections at a time, delay the second by 5-10 s on first load, all share the same conversation" ask. [`pulse/visual.tsx`](../../playground/src/pulse/visual.tsx) `runInsights` IIFE swaps the cycle-47.14 pattern (await stage 0, then concurrency-3 pool) for a single concurrency-2 pool: worker A picks stage 0 immediately, worker B waits **8 s** (`FIRST_LOAD_STAGE_1_DELAY_MS`) before its first pick (stage 1), then both drain the remainder. Cycle-47.2 single-flight opener in `obtainMessage()` is untouched, so every stage still shares the same `conversation_id`. Stop-flag honored before the delayed first pick. HEADLINE-first paint preserved by the head-start. Trims ~one stage's wall-clock vs the prior serialize-stage-0 pattern; gentler on Genie /messages rate limit (2 in-flight, not 3). Honest framing: this is **not** the 3:39 → 5-10 s leap — dominant cost is Genie per-message latency, which is upstream. Use the perfInstrumentation `console.table` from `b71270f`/`eae37a1` to see per-stage durations; further levers (stage fusion, prompt trimming, supervisor routing, foundation-model SSE for Insights) are their own cycles. Validation: `npm run lint` clean; `npm run test -- --run` **920/920** across 72 files; `npm run build` clean (17.82 s).
- **Post-UAT-1840 follow-up — glyph sweep + tooltip rollout + perf wiring (2026-05-19).** Closed the non-latency P2 items from [`docs/CODEX_VERIFY_RESULTS_2026-05-19_post-uat-1840.md`](../CODEX_VERIFY_RESULTS_2026-05-19_post-uat-1840.md). [`pulse/visual.tsx`](../../playground/src/pulse/visual.tsx): `SectionSqlPanel` raw `📋` → `<Icon name="copy" />`; fusion/SQL/MD toolbar copy buttons use the same SVG + label pattern with `<Icon name="check" />` on the copied flash; Genie audit `↻ Refresh` and per-section / assumption-note `↻ Retry` → SVG refresh + label; outer `runInsights` IIFE opens a `total` perf stage and closes it + `dumpRun()`s a `console.table` in `finally`. [`KnowledgeShell.tsx`](../../playground/src/knowledge/KnowledgeShell.tsx): active-pack `⚙` → inline SVG cog. [`SustainabilityIndicator.tsx`](../../playground/src/components/SustainabilityIndicator.tsx): reset `↻` → inline SVG refresh. [`AISidebar.tsx`](../../playground/src/components/AISidebar.tsx): Ask Pulse `ask()` opens `total` + `submit` stages; `submit` closes when the start-conversation POST returns; `polling` opens on polling kickoff; `finalize()` closes any still-open stages and dumps a per-question `console.table`. [`FieldRow.tsx`](../../playground/src/settings/primitives/FieldRow.tsx): `tip` prop now accepts either `ReactNode` (legacy) OR `StructuredTip` `{ title, body }` that renders through `HelpTip`'s title + bullet slots; backwards-compatible. Twelve dense card-level tips migrated to structured form across [`SetupGroup.tsx`](../../playground/src/settings/groups/SetupGroup.tsx) (5), [`sub/BiGovernance.tsx`](../../playground/src/settings/groups/sub/BiGovernance.tsx) (2), [`sub/AiSupervisorFusion.tsx`](../../playground/src/settings/groups/sub/AiSupervisorFusion.tsx) (3), [`sub/PreferencesAppearance.tsx`](../../playground/src/settings/groups/sub/PreferencesAppearance.tsx) (3), and [`sub/SystemDeveloper.tsx`](../../playground/src/settings/groups/sub/SystemDeveloper.tsx) (1). Validation: `npm run lint` clean; focused `HelpTip AiGroup viewportControls` **32/32**; full `npm run test -- --run` **920/920** across 72 files; `npm run build` clean (27.49s). Tripwires preserved: no Pulse-PBI compat broken (only adds `Icon` SVG + perfInstrumentation, both no-op-safe in the iframe sandbox), no Genie sandbox widening, no validator authority weakening, no reintroduced blank BI pane / `BI BI Viz` / `UniBridge` / `AI brain` / `AI-generated · Source: default`, no interactive controls inside `role="tooltip"`, no credentials in new files. Honest deferrals: **latency itself remains the carry-forward blocker** — this pass only adds visibility (DevTools Performance marks + per-run `console.table` summary), it does not make anything faster; the actual speedup is its own cycle. Pulse-PBI compat surface emoji `displayName`s in `pulse/settings.ts` and the `SettingsShell` geometric `GROUP_ICONS` rail set were intentionally left alone (coherent visual sets, Codex did not flag them). Mobile / cross-platform verification still queued.
- **Codex verification of post-final-UAT polish `b71270f` (2026-05-19).** Visible-browser verification completed against [`docs/CODEX_VERIFY_HANDOFF_2026-05-19_post-uat.md`](../CODEX_VERIFY_HANDOFF_2026-05-19_post-uat.md). Added [`docs/CODEX_VERIFY_RESULTS_2026-05-19_post-uat-1840.md`](../CODEX_VERIFY_RESULTS_2026-05-19_post-uat-1840.md) with evidence under [`docs/evidence/codex-verify-post-uat-1840/`](../evidence/codex-verify-post-uat-1840/). Validation: proxy health OK, dev server HTTP 200 on 5174, playground `npm run lint` clean, focused `HelpTip AiGroup viewportControls` **32/32**, full **920/920**, `npm run build` clean. Verified passes: HelpTip portal renders under `BODY`, stays unclipped at 599 x 694, and contains no interactive children; AI Insights section footer actions are SVG-only with accessible labels/titles; duplicate arrow/sign patterns are gone; reused SQL opens in-place as real SQL labelled `Reused from AI INSIGHTS BRIEFING`. Still not done: latency remains bad (AI Insights completed around `3:39`), tooltip content rollout is partial (dense paragraphs remain), `SectionSqlPanel` still has a raw `📋` copy button, static audit still finds raw glyph candidates in deeper/legacy surfaces, and mobile acceptance was not run.
- **HelpTip portal + SQL affordance + duplicate-arrow + Pulse footer SVGs + perf instrumentation (2026-05-19).** Closed the structural P1/P2 items from Codex's 09:14 UAT. HelpTip [primitives/HelpTip.tsx](../../playground/src/settings/primitives/HelpTip.tsx) now portal-renders the bubble into `document.body` with `position:fixed` viewport coordinates — root-cause fix for edge-tooltip clipping (was overflow:hidden / stacking-context, not z-index). Added `title` + `body` slots for short structured tooltip copy; still no interactive controls inside `role="tooltip"`. Pulse section footer in [visual.tsx](../../playground/src/pulse/visual.tsx): `📋` / `↻` / `</>` text glyphs replaced with SVG icons (clipboard / refresh / code-brackets). **SQL affordance rewrite**: when a section has no own SQL, look up sibling SQL via `stageSqlByTitle` and render in-place labelled "Reused from <SECTION>"; nothing traceable → short honest line ("No SQL available for this section. This stage produced a narrative-only output and the run did not retain a sibling query to reference") instead of the previous dead explanatory paragraph. New `stripRedundantSignForPill()` helper applied at every TrendPyramid render site so `+33.42%` no longer renders as "▲ +33.42%" duplicate-direction — closes Rajesh's two-arrow finding. Author-facing legacy `AI-generated source/timestamp footer` / `AI-generated | Source: <profile> | <relative-time>` copy in [pulse/settings.ts](../../playground/src/pulse/settings.ts) and [pulse/setupStep5.tsx](../../playground/src/pulse/setupStep5.tsx) rewritten to match the rendered "Generated by PulsePlay · Source: <profile> · Updated <relative-time>" footer. New [lib/perfInstrumentation.ts](../../playground/src/lib/perfInstrumentation.ts) Performance API utility (`stageStart`/`stageEnd` emit DevTools-visible marks; `dumpRun` prints `console.table` of stage durations) — **does NOT change latency**; per Rajesh's "don't fake speed by polishing the spinner" this is the visibility primitive only; wiring it into pipelines pairs with the actual latency cycle. Validation: TypeScript clean, `npm run lint` clean, `npm run test -- --run` → **920/920**, `npm run build` clean (14.16 s). Tripwires preserved: no Pulse-PBI compat broken, no Genie sandbox widening, no validator changes, no false validation claims, no reintroduced blank BI pane / `BI BI Viz` / `UniBridge` / `AI brain` / `AI-generated · Source: default` / interactive controls inside `role="tooltip"`. Codex testing handoff at [`docs/CODEX_VERIFY_HANDOFF_2026-05-19_post-uat.md`](../CODEX_VERIFY_HANDOFF_2026-05-19_post-uat.md). Honest deferrals carried forward: latency itself (instrumentation only here; the speedup is its own cycle), wiring instrumentation into Insights/Ask Pulse pipelines, typography token contract, mobile / cross-platform verification, P3-07/P3-08 profile picker empty when `configured:false`, EL-BIVIZ-PEER Power BI adapter shape mismatch, surface-specific knob split, component-scoped pop-out, cross-surface companion launch, theme pack lane.
- **Codex final UAT verification after Claude `d3c38be` (2026-05-19).** Visible-browser verification completed against [`docs/CODEX_FINAL_UAT_REGRESSION_HANDOFF_2026-05-19.md`](../CODEX_FINAL_UAT_REGRESSION_HANDOFF_2026-05-19.md). Added [`docs/CODEX_FINAL_UAT_RESULTS_2026-05-19-0914.md`](../CODEX_FINAL_UAT_RESULTS_2026-05-19-0914.md) with evidence under [`docs/evidence/final-uat-regression-2026-05-19-0914/`](../evidence/final-uat-regression-2026-05-19-0914/). Validation: proxy health OK, dev server HTTP 200 on 5174, playground `npm run lint` clean, focused **45/45**, full **920/920**, `npm run build` clean. Passes: main switcher no longer shows `BI BI Viz`; `/settings/ai` has Assistant / Shared context / Response behavior / Surface-specific IA; HelpTip mutual exclusion works; visible Settings no longer says `AI brain`; Ask Pulse returns Narrative/Table/SQL; AI Insights Return Rate uses physical up arrow + amber semantic tone. Still not done: response latency misses Rajesh's 5-10 second target (Ask Pulse still working around 1:22; AI Insights about 3:18), Pulse card footers still expose raw utility glyphs (`📋`, `↻`, `</>`), KPI/trend deltas can show duplicate nested arrows, author-facing Pulse setup/settings copy still references legacy `AI-generated | Source: <profile> | <relative-time>`, edge HelpTips can still go under pane frames / clipping containers and need a top-level floating layer plus title/body formatting, and mobile/cross-platform verification remains queued.
- **Final naming/glyph cleanup + HelpTip mutual exclusion + UAT handoff (2026-05-19).** Closed every P1/P2 from Codex's 08:31 verify. `AI brain` → `AI assistant` in [AiGroup.tsx](../../playground/src/settings/groups/AiGroup.tsx) Provider helper and [FirstRunWizard.tsx](../../playground/src/components/FirstRunWizard.tsx) tools subtitle. Visible decorative-glyph text replaced with aria-hidden SVG icons: `⚙` in Settings header → SVG cog; `← Back to app` in [SettingsShell.tsx](../../playground/src/settings/SettingsShell.tsx) + [KnowledgeShell.tsx](../../playground/src/knowledge/KnowledgeShell.tsx) → SVG arrow + label; `⚡ Test proxy` / `⚡ Test selected profile` in [TestButton.tsx](../../playground/src/settings/primitives/TestButton.tsx) → SVG lightning bolt; `Full embed form →` → "Open full embed settings"; `Databricks docs ↗` → label + SVG external-link icon; `Tune Insights behavior →` → "Tune Insights behavior"; `Browse pack content ↗` → label + SVG external-link icon; FooterLink `→` arrow dropped — all in [SetupGroup.tsx](../../playground/src/settings/groups/SetupGroup.tsx). Removed unused [renderInsightsProvenance()](../../playground/src/pulse/visual.tsx) helper that still contained legacy `AI-generated / Source: default` copy; replaced with explanatory comment so future audits don't keep flagging the string. HelpTip [primitives/HelpTip.tsx](../../playground/src/settings/primitives/HelpTip.tsx) gained module-level mutual-exclusion: opening a tooltip closes any other open tooltip; document-level pointerdown closes all open tips on outside clicks; `aria-expanded` added to trigger. Validation: TypeScript clean, `npm run lint` clean, `npm run test -- --run` → **920/920**, `npm run build` clean (15.77 s). Tripwires preserved: no Pulse-PBI compat broken, no Genie sandbox widening, no validator changes, no false validation claims, no reintroduced blank BI pane / `BI BI Viz` / `UniBridge` / `AI brain` / `AI-generated · Source: default` / interactive controls inside `role="tooltip"`. Comprehensive UAT/regression handoff at [`docs/CODEX_FINAL_UAT_REGRESSION_HANDOFF_2026-05-19.md`](../CODEX_FINAL_UAT_REGRESSION_HANDOFF_2026-05-19.md) — smoke commands, 10-route checklist, 12-interaction checklist, 15 AI UAT questions (10 core + 5 edge), data-correctness reconciliation rubric, 7-persona checklist, regression matrix, evidence folder convention, credential-redaction rules, honest known-gap carry-forward, result file format. Still deferred (carried forward): P3-07/P3-08 (profile picker empty when `configured:false`), EL-BIVIZ-PEER (Power BI adapter shape mismatch), tooltip rewrite on deep Settings subroutes, Launchpad raw-ID demotion, surface-specific knob split, component-scoped pop-out, cross-surface companion launch, theme pack lane.
- **Codex verification of Claude IA/copy polish (2026-05-19).** After Claude commit `7562efd`, Codex visibly verified `/`, `/settings/setup`, `/settings/ai`, `/settings/preferences`, `/settings/bi`, `/knowledge`, and `/workbench` in the in-app browser. Added [CODEX_IA_UI_POLISH_VERIFY_2026-05-19-0831.md](../CODEX_IA_UI_POLISH_VERIFY_2026-05-19-0831.md) with evidence under [docs/evidence/codex-ia-ui-polish-verify-0831/](../evidence/codex-ia-ui-polish-verify-0831/). Passes: `BI BI Viz` removed from root/Pulse switcher; AI Settings now has Assistant / Shared context / Response behavior / Surface-specific behavior; shared context houses Knowledge Pack, Vector Search, and UC Metric View; blocked AI profile state explains itself; HelpTip visible bubbles did not clip and contained no interactive controls. Remaining gaps: `/settings/ai` still says `AI brain`, Settings still exposes text glyphs (`⚙`, `←`, `⚡`, `↗`, `→`), unused `renderInsightsProvenance()` still contains stale `AI-generated / Source: default` copy, and multiple clicked HelpTips can leave an offscreen stale `role="tooltip"` node. Validation: playground lint clean and full **920/920** tests passing.
- **IA restructure + tooltip + provenance + emoji-glyph fixes (2026-05-19).** Implemented the focused UI/IA polish pass following Codex's three 2026-05-19 audits (tough run, tooltip, naming). Restructured `AiGroup.tsx` into the 4-tier IA Codex recommended: **Assistant** (Provider · Model/Agent · Connection test), **Shared context** (Knowledge pack · Vector Search KB · UC Metric View · Browse library), **Response behavior** (Pulse genieSettings panel with "Shared with both AI Insights and Ask Pulse" helper), **Surface-specific behavior** (Supervisor Fusion + Knowledge Base deep-links). Fixes the mental-model bug where Knowledge Pack appeared to be AI-Insights-only. Replaced literal "BI" / "✨" / "💬" text inside Pulse internal header tabs in `visual.tsx` with SVG glyphs matching `SurfaceSwitcher` — closes Codex's `P1-13` / `EL-SWITCHER-COPY` `BI BI Viz` regression. Rewrote provenance footer: `AI-generated · Source: default · 19 min ago` → `Generated by PulsePlay · Source: Default profile · Updated 19 min ago` via new `formatProvenanceSourceLabel` helper; no "100% hallucination-free" claim added. Tooltip primitives: `HelpTip.tsx` now viewport-aware (measures trigger rect on open/resize, picks `center/left/right` align + `above/below` side, clamps width); CSS gets matching variant classes. Authoring contract locked: no interactive controls inside `role="tooltip"`. SetupGroup's AI profile "Databricks docs ↗" link moved out of tooltip into new `labelTrailing` slot on `FieldRow.tsx`. SetupGroup AI profile hint now state-aware: explains the blocked state when proxy is online but profile select is empty. BiGroup `SubSection` h3 no longer `text-transform:uppercase`; sentence-case labels render naturally. Validation: TypeScript clean, `npm run lint` clean, `npm run test -- --run` → **920/920** passing, `npm run build` clean. Tripwires preserved: no Pulse-PBI sibling compat broken; no Genie sandbox widening; no validator changes. Codex handoff at [`docs/CODEX_IA_UI_POLISH_HANDOFF_2026-05-19.md`](../CODEX_IA_UI_POLISH_HANDOFF_2026-05-19.md). Still deferred: `P3-07`/`P3-08` (profile picker empty when `configured:false`) and `EL-BIVIZ-PEER` (Power BI adapter contract mismatch with Quick Setup paste-in) — both are data-layer fixes outside this cosmetic/IA pass.
- **Tough visible UI run v2 + tooltip + naming/copy audits (2026-05-19).** Codex ran Claude's v2 persona × element × break-it plan in the already-open visible in-app browser, per Rajesh's request to watch the UI test live. Pre-flight passed: proxy health OK, dev server OK, playground **920/920**, `npm run lint` clean. Added [TOUGH_TEST_RESULTS_2026-05-19-1253.md](../TOUGH_TEST_RESULTS_2026-05-19-1253.md) and evidence under [docs/evidence/tough-test-2026-05-19-1253/](../evidence/tough-test-2026-05-19-1253/). Executed **83 visible scenarios**: **71 PASS / 5 FAIL / 1 SKIPPED / 6 N/A**, no Critical product failures. Failures locked for Claude: duplicate `BI BI Viz` switcher text, Setup AI profile select remains unpopulated/not selectable after proxy reports 2 profiles, and configured autoAuth Power BI fixture can fall into an adapter access-token error. Rajesh then asked for tooltip coverage; added [TOOLTIP_AUDIT_2026-05-19-1310.md](../TOOLTIP_AUDIT_2026-05-19-1310.md) with evidence under [docs/evidence/tooltip-audit-2026-05-19-1310/](../evidence/tooltip-audit-2026-05-19-1310/). Tooltip gaps: compact-width bubble clipping, interactive link inside non-interactive tooltip, and AI profile tooltip not explaining the blocked profile-select state. Rajesh also asked for section/tool naming and special-character polish; added [NAMING_AND_COPY_AUDIT_2026-05-19-1320.md](../NAMING_AND_COPY_AUDIT_2026-05-19-1320.md) with evidence under [docs/evidence/naming-audit-2026-05-19-1320/](../evidence/naming-audit-2026-05-19-1320/). Naming gaps: decorative glyphs in visible/accessibility labels, duplicated `BI BI Viz`, internal terms such as `AI brain`, `powerbi`, `Proxy ok`, `Security strict`, `v0`, and generated-output provenance that needs to become human-readable and trust-oriented. Rajesh also called out a Settings IA correction: Knowledge Pack, domain guidance, metric semantics, and grounding are shared by AI Insights and Ask Pulse; the audit now recommends progressive grouping under `AI > Assistant`, `AI > Shared context`, `AI > Response behavior`, and `AI > Surface-specific behavior`. Purpose model: AI Insights is the proactive briefing, Ask Pulse is conversational follow-up, and BI Viz is the observed source surface.
- **Visible extreme E2E partial + unified companion UX handoff (2026-05-19).** Codex ran the extreme catalog pre-flight and a visible in-app browser E2E slice after Rajesh requested observable testing only. Pre-flight passed: proxy health OK, dev server OK, playground **918/918**, `npm run lint` clean. Catalog files parse to **2,604** scenarios, not the pasted **2,544**. Added [EXTREME_E2E_RESULTS_2026-05-19-1146.md](../EXTREME_E2E_RESULTS_2026-05-19-1146.md), evidence screenshots under [docs/evidence/visible-e2e-2026-05-19-1146/](../evidence/visible-e2e-2026-05-19-1146/), [CLAUDE_FOCUSED_GAP_BACKLOG_2026-05-19.md](../CLAUDE_FOCUSED_GAP_BACKLOG_2026-05-19.md), and [CODEX_TO_CLAUDE_SURFACE_UX_PROMPT_2026-05-19.md](../CODEX_TO_CLAUDE_SURFACE_UX_PROMPT_2026-05-19.md). Extended visible pass covered Settings Setup/BI/AI/Preferences/System/Advanced, Knowledge, Launchpad, Workbench, root AI Insights, root Ask Pulse, and root BI Viz. Follow-up locked: `BI Viz` must become a true peer surface, surface switcher icon/label treatment needs smoothing, mobile Dock is offscreen, dock/undock must be component-scoped, and every screen needs cross-surface companion launch so users never feel blocked or lose task context.
- **Legacy split-state migration for blank BI pane (2026-05-18).** Rajesh still saw the blank BI pane next to AI because old localStorage stored `pulseplay:enabled-components=both`. Added one-time migration in [App.tsx](../../playground/src/App.tsx) and [settingsStore.tsx](../../playground/src/settings/settingsStore.tsx): legacy `both` becomes unified `mix`, with a migration marker so explicit Split + Mix persists afterward. Added regression coverage in [viewportControls.integration.test.tsx](../../playground/src/__tests__/viewportControls.integration.test.tsx). Validation: focused viewport **19/19**, `npm.cmd run lint` clean. Note: unrelated Workbench files were already dirty and left untouched.
- **KPI delta arrows physical, tone semantic (2026-05-18).** Rajesh clarified that Return Rate increasing should show an up arrow because the number increased; the color should follow the explicit status/business cue, e.g. amber for `🟡 Watch`. Updated [visual.tsx](../../playground/src/pulse/visual.tsx) so KPI delta cue direction is physical movement, while delta tone uses explicit status tone when present and only falls back to metric-direction tone when status is absent. Updated [insightsRendererPolish.test.tsx](../../playground/src/pulse/__tests__/insightsRendererPolish.test.tsx) to lock Return Rate up+amber, Profit Margin down+amber, and no-status metric-direction fallback. Validation: focused renderer **6/6**, `npm.cmd run lint` clean. Note: unrelated Workbench files were already dirty and left untouched.
- **Playwright browser smoke enabled for unified BI Viz (2026-05-18).** Installed `@playwright/test` in [playground/package.json](../../playground/package.json) and installed Playwright Chromium locally. Fixed [FirstRunWizard.tsx](../../playground/src/components/FirstRunWizard.tsx) to spread boolean `inert`, and hardened [PulseShell.tsx](../../playground/src/components/PulseShell.tsx) nested-root cleanup so real surface switches defer `Visual.destroy()` while React dev StrictMode reuses the pending Pulse visual during simulated remount. Real Chromium smoke at `http://127.0.0.1:5173/` now passes clean: `AI Insights` -> `BI Viz` -> `AI Insights`, `focus=split`, BI panel ready, 1 secure Power BI iframe, 0 console/page errors. Validation: focused **23/23**, lint clean, full playground **745/745**, `git diff --check` clean except LF/CRLF warnings.
- **Unified BI Viz tab + metric-direction cue cleanup (2026-05-18).** Rajesh clarified that `AI Insights`, `Ask Pulse`, and `BI Viz` are peer navigation surfaces in the same primary canvas. Updated [App.tsx](../../playground/src/App.tsx) so `enabledComponents="mix"` swaps the primary canvas to the BI pane when `BI Viz` is clicked instead of entering focused-pane mode or writing `?focus=bi`; the BI surface now has a compact `AI Insights | Ask Pulse | BI Viz` switcher to return to Pulse surfaces. Added a PulseShell → Pulse visual tab-request bridge for returning directly to AI Insights/Ask Pulse. Tightened KPI delta rendering so lower-is-better unfavorable movement such as Return Rate `+0.4pp (▲ +6.3%)` cannot render a nested green inner trend pill. Validation: focused viewport + insights renderer tests **23/23**, `npm.cmd run lint`, and `git diff --check` clean except LF/CRLF warnings. Follow-up 2026-05-18 installed Playwright and closed the real-browser smoke gap.
- **App feature/option audit (2026-05-18).** Added [APP_FEATURE_OPTION_AUDIT.md](../research/APP_FEATURE_OPTION_AUDIT.md), a read-code audit matrix across the main app shell, first-run wizard, Settings groups, embed configuration, BI adapters, assistant/chat surfaces, unified workbench in-flight files, Launchpad, Knowledge Base, proxy-facing route coverage, visualization gap, and priority fill plan. Verdict: the two-axis foundation is solid, but the flagship chat is still behind; the right solve is the locked Unified Ask Pulse Workbench with Native Genie / PulsePlay Verified / Hybrid modes, plus a no-ungrounded-artifacts validation contract. Main P0 gaps called out: Settings BI Provider read-only, Launchpad Genie actions overpromise, Advanced Reset BI misses `pulseplay:bi-embed-config`, managed-agent/per-tile options are placeholders, non-PBI vendor adapters are iframe stubs, and the visualization reference is not yet runtime support. Workbench Steps 2-4 are tracked component/validator foundations but not yet app-routed. Concurrent uncommitted Step 5 chart-registry/renderer/compiler/package work is visible locally and should be landed or discarded intentionally. No code changes in this audit.
- **React Query foundation proof slice audited + repaired (2026-05-18).** Rajesh/Copilot committed `5b51fc2` with `@tanstack/react-query`, a shared `apiFetch()` wrapper, `queryClient`, and `useAllowlist()` / `usePacks()` integration in `App.tsx`. Codex independently audited the external patch and kept it, but fixed three acceptance gaps: React Query Devtools are now dev-only/test-skipped and moved to `devDependencies`; allowlist query retries are disabled so governance failures fail closed promptly; packs-loaded semantics now match the old "success or error means loaded" behavior. Added `playground/src/vite-env.d.ts`, hardened `apiFetch()` with a request-id fallback, and expanded `appGovernance.integration.test.tsx` to cover query cache population plus failed allowlist -> fail-closed alert. Validation: playground lint, appGovernance **2/2**, viewportControls.integration **18/18**, full playground **580/580**, build, production bundle scan for no React Query Devtools strings, and Vite root smoke HTTP **200** at `http://127.0.0.1:5173/`.
- **KPI delta cues respect metric direction (2026-05-17; superseded nuance 2026-05-18).** Initial slice separated overall KPI tile status from delta-direction tone. Rajesh later clarified the final UX rule: arrow direction is physical movement, while color/tone carries explicit status or metric-direction meaning. See the newer 2026-05-18 session entry above for the current behavior.
- **Phase 11b read-side: labelled SQL sections in Pulse (2026-05-17).** After proxy commit `8e29260` surfaced parsed Genie SQL markers at `attachments[].query.sqlSections`, [genie.ts](../../playground/src/pulse/genie.ts) now lifts those fragments into `GenieMessage.sqlSections`, and [visual.tsx](../../playground/src/pulse/visual.tsx) renders labelled SQL section tabs ahead of the raw `sqlQuery/sqlQueries` fallback. Added [genieSqlSections.test.tsx](../../playground/src/pulse/__tests__/genieSqlSections.test.tsx) for attachment lifting and labelled rendering. Validation: focused **3/3**, `npm.cmd run lint`, full playground **571/571**, `npm.cmd run build`. Remaining gap: Foundation Model response-path extraction symmetry is still unverified.
- **Slice 1b Problem Details foundation + malformed-body hardening (2026-05-17).** Added [problemDetails.js](../../proxy/lib/problemDetails.js) as the RFC 9457-style backend envelope helper with PulsePlay extensions, redaction, request-id fallback, upstream mapping, and `sendProblem()` streaming carve-out. [server.js](../../proxy/server.js) now returns `application/problem+json` for malformed JSON/body-too-large failures and has a final unexpected-error fallback using the locked sentinel `PulsePlay could not complete this request. Share the support code with your administrator.` Body parsing runs after the CORS/security header layer so malformed-body responses still include browser-visible diagnostics. Validation: `node --check server.js`, `node --check lib/problemDetails.js`, focused proxy **150/150**, full proxy **740/740**. Remaining error lane: raw `err.message` route migration, Databricks OAuth normalization, and streaming in-band error events.
- **H1 doc sync: ResponsesAgent is backend path #9 (2026-05-17).** Claude flagged that `/responses-agent/*` had shipped but active docs still carried the old runtime-backend count. Updated [CLAUDE.md](../../CLAUDE.md), [ARCHITECTURE.md](../ARCHITECTURE.md), [PROXY_REFERENCE.md](../PROXY_REFERENCE.md), [README.md](../../README.md), [ROADMAP.md](../ROADMAP.md), [SECURITY_ARCHITECTURE.md](../SECURITY_ARCHITECTURE.md), [CONNECTOR_PROBE_AND_SMART_CONNECT.md](../CONNECTOR_PROBE_AND_SMART_CONNECT.md), and [AGENT_SYNC.md](../AGENT_SYNC.md) to use nine and include Mosaic AI ResponsesAgent. Historical audit/migration snapshots remain unchanged. Validation: current-count references in active docs now say nine; `git diff --check` clean except CRLF warnings.
- **Navigation styling pass: unified surface switcher rail (2026-05-17).** Rajesh asked to start styling improvements with navigation. The AI Insights / Ask Pulse / BI Viz row now uses a shared `gn-surface-switcher` wrapper instead of one active blue pill plus two loose labels. AI Insights and Ask Pulse stay semantic tabs; BI Viz remains a focus button that dispatches the existing viewport event. Styling adds icon wells, inactive affordances, hover elevation, active gradient/shadow, dark-mode treatment, compact wrapping, and forced-colors borders. Canva brand-template search for `dashboard navigation ui` returned no useful templates, and the available Figma team library had no segmented navigation component, so the local PulsePlay language was used. Validation: playground lint clean, focused viewport controls **18/18**, build clean, full playground **568/568**, browser smoke at `http://127.0.0.1:5176/`.
- **Error handling baseline locked + ResponsesAgent Slice 1a hotfix (2026-05-17).** Rajesh accepted the no-panic error-handling strategy lane, and Claude independently confirmed the P0 `/responses-agent/*` middleware gap. Promoted [ERROR_HANDLING_STRATEGY.md](../ERROR_HANDLING_STRATEGY.md) to a locked decision, folded in Claude's challenge, and split the roadmap into Slice 1a and Slice 1b. Runtime hotfix: [server.js](../../proxy/server.js) now mounts `/responses-agent` under rate-limit, IdP, shared-key, and allowlist middleware. [server.test.js](../../proxy/tests/server.test.js) now locks rate-limit/shared-key/allowlist structural coverage and verifies `/responses-agent/health` rejects missing shared keys. Validation: `node --check server.js`, focused proxy `npm.cmd test -- server --runInBand` **133/133**, full proxy `npx.cmd jest --runInBand --verbose` **723/723**. Residual P0s remain open: malformed JSON/no support code, raw `err.message` leaks, and Databricks OAuth error normalization.
- **Unified surface correction: BI Viz peer action (2026-05-17).** Rajesh correctly flagged that the prior Option A LayoutPreset facade still left BI as a separate permanent right pane. Corrected `enabledComponents="mix"` so it is now the unified/default surface mode: AI Insights / Ask Pulse own the main surface and BI is opened on demand via a new Pulse-row `BI Viz` action that focuses the existing BI pane. `enabledComponents="both"` remains the explicit split-pane mode behind `Split + Mix`. Settings Preferences copy now says `Unified` / `Split`. Validation: focused playground `viewportControls.integration` + `layoutPresets` **33/33**, Settings drift follow-up **18/18**, lint clean, full playground **568/568**, browser smoke on `http://127.0.0.1:5176/` confirmed `AI panel=1`, `BI panel=0`, `BI Viz=1` before click and focused BI after click. Residual note: existing browsers with `pulseplay:enabled-components=both` stay split until the author switches to Balanced/Unified; no forced localStorage migration was added.
- **Error handling strategy and no-panic failure contract (2026-05-17).** Rajesh asked for a deep multi-agent scan so errors become clear, root-cause-oriented, and resolvable. Codex coordinated four scan lanes: frontend/BI adapter audit, proxy/API audit, Databricks/connector taxonomy, and source-backed modern error handling. Added [ERROR_HANDLING_STRATEGY.md](../ERROR_HANDLING_STRATEGY.md) with RFC 9457 Problem Details as the recommended API envelope, PulsePlay categories, UX copy rules, P0/P1/P2 gaps, phased roadmap, and acceptance criteria. The initial scan was docs-only; P0-1 is now closed by the newer ResponsesAgent Slice 1a hotfix above.
- **Ask Pulse label + unified surface-tabs proposal (2026-05-17).** Rajesh proposed collapsing the default screen into one surface switcher instead of keeping BI as a permanently separate section. Codex read [Proposed_Preset_Templates.pdf](../Proposed_Preset_Templates.pdf), interpreted T1-T5 as `AI Insights | Ask Pulse | BI Viz`, future fused `AI/BI Insights`, BI-only, AI-Insights-only, and Ask-Pulse-only templates, and captured the architecture recommendation in [AGENT_SYNC.md](../AGENT_SYNC.md). Visible `Chat` copy is now `Ask Pulse` across the Pulse viewer tab, setup wizard, Settings AI/Preferences selectors, and format-pane display strings; internal `chat` keys are unchanged. Follow-up proposal adds an in-app floating comparison layer so any primary tab can show another surface as a companion panel, distinct from the existing detached popup-window `Float` action. A second follow-up captures Rajesh's Grammarly-style screenshot as a `PulseBubble` launcher decision candidate: persistent scroll-context access to `AI Insights`, `Ask Pulse`, `BI Viz`, and `Compare`, with safe snap anchors, no ambiguous badge count, and accessibility/collision guardrails. Validation: playground `npm run lint`, full playground **552/552**, and `git diff --check` passed for the code rename; Pulse Bubble research is doc-only. The BI-as-tab/floating-layer/bubble architecture is proposal-stage only; no BI adapter or viewport architecture changed.
- **Databricks Launchpad + P2-P8 enablement (2026-05-17).** Codex continued from Claude's handoff and validated against Rajesh's live Databricks workspace. Added [databricksEnablement.js](../../proxy/lib/databricksEnablement.js), live proxy routes for AI/BI dashboards, Genie Spaces, serving endpoints, Apps, SQL warehouses, UC metric views, metric-view detail, Vector Search query, and the `databricks-aibi` token flow in [server.js](../../proxy/server.js). Added `databricks-aibi` and `databricks-genie` adapters under [bi-adapters](../../bi-adapters), `/launchpad` in [LaunchpadShell.tsx](../../playground/src/launchpad/LaunchpadShell.tsx), Databricks Settings fields for Vector Search KB + UC Metric View, the first [EvidenceDrawer.tsx](../../playground/src/components/EvidenceDrawer.tsx) slice, [app.yaml](../../app.yaml), and [DEPLOY_DATABRICKS_APP.md](../DEPLOY_DATABRICKS_APP.md). Live discovery found 7 Genie Spaces, 2 AI/BI dashboards, 13 serving endpoints, 1 App, 1 SQL warehouse, 0 Vector Search endpoints, and 2 metric views. Validation: proxy **684/684**, playground lint, Launchpad focused **2/2**, playground **552/552**, build, browser smoke at `/launchpad`. Remaining gaps: Genie embed still needs a Databricks-generated iframe/src, Vector Search retrieval waits on an approved index, Evidence Drawer needs lineage/metric/vector citations, and the optional `@databricks/aibi-client` SDK is runtime-only until dependency installation is approved.
- **Databricks capability registry P1 (2026-05-17).** Codex picked up Claude's Launchpad handoff from `.claude/worktrees/gallant-jones-a71415/docs/CODEX_TASK_DATABRICKS_LAUNCHPAD.md` and scoped the work to P1 only. Added [databricksCapabilityRegistry.js](../../proxy/lib/databricksCapabilityRegistry.js), wired `/assistant/capabilities` in [server.js](../../proxy/server.js) to return a 5-minute cached Databricks capability snapshot, added [databricksCapabilities.ts](../../playground/src/lib/databricksCapabilities.ts), and gated Settings › AI › Vector Search KB in [AiGroup.tsx](../../playground/src/settings/groups/AiGroup.tsx) so it renders only when Vector Search is ready and endpoint count is greater than zero. Validation: proxy focused **5/5**, server **119/119**, combined focused **124/124**, full proxy **680/680**, playground focused **11/11**, lint, full playground **531/531**, build, diff-check. P2-P8 were open at that commit; the follow-on enablement pass above folds in the previously untracked `bi-adapters/databricks-aibi/` directory.
- **Pulse primary surface streamlined + backend canvas policy (2026-05-16).** Rajesh pointed at the Pulse AI pane's BI Tool dropdown, row-level `Open setup`, repeated BI source status, visible `Console` button, empty toolbar space, and visible `BI tiles: 1 / 2 / 4` controls. Codex removed the Pulse-mode BI source row from [App.tsx](../../playground/src/App.tsx), removed the visible Console trigger from [visual.tsx](../../playground/src/pulse/visual.tsx), added compact AI pane actions beside AI Insights / Chat (maximize/restore, minimize, open page, refresh), and moved BI tile count behind backend policy `allowlist.display.biTileMode`. Settings now shows canvas tiles read-only. Regression coverage in [viewportControls.integration.test.tsx](../../playground/src/__tests__/viewportControls.integration.test.tsx) asserts no duplicate BI/setup/Console/tile toolbar is visible and the new icon controls exist. Validation: playground lint, focused settings/viewport **43/43**, full playground **503/503**, build, proxy focused **22/22** + **119/119**, full proxy **675/675**, diff-check.
- **Setup readiness pill + Settings setup tree (2026-05-16).** Rajesh refined the LIFO decision: keep one top-right pill, but make it an app-owned setup/readiness entry that opens Settings › Setup. Codex added [setupReadiness.ts](../../playground/src/settings/setupReadiness.ts), [SetupGroup.tsx](../../playground/src/settings/groups/SetupGroup.tsx), made `/settings` default to Setup, added the top-right `Ready` / `Setup needed` pill in [App.tsx](../../playground/src/App.tsx), removed the unused floating settings gear/toggle functions, and repointed Pulse Console/BI source handoffs to `/settings/setup`. Validation: focused **55/55**, playground lint, full playground **502/502**, build, diff-check. [AGENT_SYNC.md](../AGENT_SYNC.md) also captures the next interaction-workbench + semantic-layer research lane for Claude review.
- **Settings owns configuration; Console owns status (2026-05-16).** Rajesh flagged the floating top-right `Not connected | Managed` setup/status pill as unnecessary global chrome, then clarified that duplicated setup functions should be streamlined into the full-page Settings surface. Codex removed the fixed Pulse status/scope pills from Pulse global chrome, added an in-pane `Console` trigger in [visual.tsx](../../playground/src/pulse/visual.tsx), moved connection/scope guardrail chips into the centered Developer Tools header, retired the reachable Console Setup/Display editors, added [pulseVisualSettingsStore.ts](../../playground/src/settings/pulseVisualSettingsStore.ts), replaced the Settings `AI Insights setup ↗` placeholder with real Settings › AI › AI Insights controls, and mirrored Settings provider selection to Pulse runtime `genieSettings.assistantProfile`. Canva sidecar reference board: design `DAHJ1oFh42k`.
- **Wizard repeat-ask + zero-vendor guard + copy-link polish (2026-05-16).** Codex closed the latest three review gaps after Claude's changes. [AISidebar](../../playground/src/components/AISidebar.tsx) now supports `AutoSubmitQuestionEvent { id, question }`, and [App.tsx](../../playground/src/App.tsx) increments an event id for every wizard `Done & ask` completion so the same suggested question can be intentionally asked on a later wizard re-run. [FirstRunWizard](../../playground/src/components/FirstRunWizard.tsx) keeps `vendorsAvailable=false` as a hard stop even under `forceWizard()`. [BiGroup](../../playground/src/settings/groups/BiGroup.tsx) uses plain `Copy link` / `Copied` labels for settings leaf deep links. [AGENT_SYNC.md](../AGENT_SYNC.md) now contains the claim/done handoff for Claude review.
- **4-step first-run setup wizard + P1 security hardening (commits `4ba76b3` + `735eb87`).** Full-bleed onboarding modal at `playground/src/components/FirstRunWizard.tsx`. Four steps: Welcome+Persona (Analyst/Executive/Developer/Designer with `applyPersonaDefaults` seeding `uiMode` + `layoutMode` + connector hint) / Choose tools (vendor + connector cards) / Connect (EmbedConfigForm + optional connectivity probe via `/api/assistant/probe`) / Explore (PackPicker + suggested-question textarea). Right-side step rail, CSS-only slide+fade, draft persistence (`pulseplay:wizard-draft`), focus-trap with `inert` on hidden panes, aria-live step announcements. "Just give me defaults" fast-lane skips to Done with Analyst preset + first vendor. Settings → System → "Re-run setup wizard" via new `forceWizard()` helper that sets `WIZARD_FORCE_KEY` (single-use, consumed by `clearDraft()` on Done/Skip). P1 hardening closes Codex's 4 RISK-P1 findings: 4.1 draft schema validation (persona checked against `VALID_PERSONA_KEYS`, step clamped 0–3), 4.3 focus-trap leakage (`inert` attribute), 4.4 probe URL bypassing Vite proxy (always `POST /api/assistant/probe`), 4.5 Re-run wizard broken for configured users. 38 wizard vitest cases, **478/478** playground total. tsc clean.
- **Roadmap reorganized around 5 parallel TRACKS (commit `5a57e7c`).** [docs/ROADMAP.md](../ROADMAP.md) replaced the linear v0.1→v1.2 version sequence with five parallel tracks (Foundation, Surface, Reasoning, Experience, Trust). Each track lists current DONE state + next milestones (no internal ordering) + "What stays modular" rule + explicit "Non-Databricks proof point" deliverable. 8 anchor modularity guarantees codified at the bottom: every surface implements BIAdapter, every connector accepts the same Prompt IR, `visibleVendors` is the only filter, Databricks features gracefully no-op when Databricks is absent, Power BI bridge stays alive, Tableau/Qlik/Looker adapters do NOT get deleted, personas are surface/connector-agnostic, wizard doesn't assume Databricks. Cross-track dependencies marked LOOSE vs HARD so most milestones can ship in any order. Legacy v0.x version labels preserved at the bottom for backward compat.
- **Agent coordination scratchpad.** Added [docs/AGENT_SYNC.md](../AGENT_SYNC.md) as a repo-tracked communication file for Codex and other agents to align on operating instructions, claims, blockers, handoffs, review risks, missing gaps, copy-paste prompts, and open questions. It is intentionally non-canonical; durable decisions still move into HANDOVER, AGENDA, ADRs, or focused feature docs.
- **Knowledge Base source governance (current docs/research pass).** Added [docs/KNOWLEDGE_BASE_SOURCE_GOVERNANCE.md](../KNOWLEDGE_BASE_SOURCE_GOVERNANCE.md), updated [pulsepacks/PACK_SPECIFICATION.md](../../pulsepacks/PACK_SPECIFICATION.md), [docs/KNOWLEDGE_BASE_ARCHITECTURE.md](../KNOWLEDGE_BASE_ARCHITECTURE.md), [docs/PACKS.md](../PACKS.md), [docs/CHAT_VISUALIZATION_KNOWLEDGE_BASE.md](../CHAT_VISUALIZATION_KNOWLEDGE_BASE.md), and partially converted [pulsepacks/cpg-fmcg/knowledge-base/references.md](../../pulsepacks/cpg-fmcg/knowledge-base/references.md) to source-card tables. Two read-only research agents validated chart-source authenticity and KB module provenance requirements. [docs/AGENT_SYNC.md](../AGENT_SYNC.md) now asks Claude to review source tiers and pack-linter rules before runtime ingestion.
- **Chat visualization knowledge base (current docs pass).** Added [docs/CHAT_VISUALIZATION_KNOWLEDGE_BASE.md](../CHAT_VISUALIZATION_KNOWLEDGE_BASE.md) as the Chat-facing chart rule baseline: legacy and modern chart rules, question-to-chart mapping, migration rules, dashboard composition, persona defaults, and proposed `ChartKnowledgeRule` shape. [docs/AGENT_SYNC.md](../AGENT_SYNC.md) now asks Claude to review storage shape: static TypeScript seed, PulsePack YAML, or `DomainContextProfile.visualizationGuidance`.
- **Common AI context model (current code/docs pass).** Added [docs/AI_CONTEXT_CONFIGURATION_MODEL.md](../AI_CONTEXT_CONFIGURATION_MODEL.md) to define one Knowledge Base-derived domain context feeding AI Insights and Chat. Updated Pulse setup Section A so common AI context is shared across AI Insights/Chat tabs, domain options derive from preset-library domains, related section/metric presets group first, and metric presets can seed the selected domain when blank.
- **Setup/settings relationship audit and control depth (current docs/UI polish pass).** Added [docs/SETUP_SETTINGS_RELATIONSHIP_AUDIT.md](../SETUP_SETTINGS_RELATIONSHIP_AUDIT.md) as the relationship-map baseline for BI/AI/knowledge setup dependencies, connector readiness, state ownership drift, progressive setup flow, and first implementation slices. Also added a subtle shared 3D/depth treatment for dropdowns/textareas in [styles.css](../../playground/src/styles.css) and the first-run wizard suggested-question textarea.
- **Modular integration architecture (current docs pass).** Added [docs/MODULAR_INTEGRATION_ARCHITECTURE.md](../MODULAR_INTEGRATION_ARCHITECTURE.md) as the planning baseline for "integrated experience, modular capability fabric." It defines the stable spine, swappable block catalog, block manifest/lifecycle, capability registry, add/remove protocol, linear-plus-wide-spectrum roadmap, and memory/state position. [ARCHITECTURE.md](../ARCHITECTURE.md), [HANDOVER.md](../HANDOVER.md), and [AGENT_SYNC.md](../AGENT_SYNC.md) now point other agents to the same plan.
- **Structured authoring standard (current docs pass).** Added [docs/STRUCTURED_AUTHORING_STANDARD.md](../STRUCTURED_AUTHORING_STANDARD.md) so prompt/guidance textareas evolve into guided structured editors with required sections, parameter chips, validation, and compiled middleware previews. This should become one reusable component family, not separate one-off textareas.
- **Phase 1 (allowlist runtime).** Added `proxy/lib/allowlist.js`, production startup validation, filtered `/assistant/allowlist` + `/assistant/profiles`, route guards, rejection audit events, Power BI workspace/report/tenant checks, and MVP 0.2 `proxy/config.example.json`.
- **Phase 7 (pack registry, pulled forward).** Added `proxy/lib/packRegistry.js` + `GET /assistant/knowledge/packs`, filtered by `allowlist.packs`.
- **Playground governance wiring.** Vendor/packs are filtered, `EmbedConfigForm` validates embed origins/workspace/report/tenant, and `BIPanel` refuses to mount a non-allowlisted URL.
- **Phase 2 (Settings shell + store) — beast-mode one.** Added [playground/src/settings/](../../playground/src/settings/): path-based router (no new dep), `SettingsProvider` + `useSettings` with allowlist-aware setters + orphan detection on load, `SettingsShell` with header/search/status strip/5-group left rail/content pane, 5 group surfaces (Preferences fully wired live; BI/AI/System/Advanced show structure + read-only current values). App.tsx now routes between playground and settings; `Cmd/Ctrl+,` opens settings; the legacy gear popover gets an "Open full Settings →" footer link.
- **Phase 3 + L7 cleanup — beast-mode two.** L1 cleanup: `pbiAuth.signInAndPrepareEmbed` now refuses non-allowlisted tenants before MSAL init (new `PbiAllowlistError`). L2 cleanup: `allowedOrigins` field on adapter `BIEmbedConfig`; generic-iframe + powerbi secure-embed paths reject unauthorized hostnames inside `mount()`. L3 cleanup: secure-embed URL paste now parses + validates `groupId` AND `reportId` against allowlist. License posture readout in BI › Status + System › Security; Fabric=false yellow banner. **L7 closed** via Vite plugin [playground/vite.cspFromAllowlist.ts](../../playground/vite.cspFromAllowlist.ts) generating strict CSP from `proxy/config.json` allowlist (no wildcards, no `'unsafe-eval'`).
- **Phase 4 + L6/L8 — beast-mode three.** `activeAiProfile` state in settingsStore with allowlist gate + orphan detection. AiGroup fully wired: Provider picker filtered by allowlist with Supervisor badge; `/assistant/profiles` extended with `type`/`spaces`/`agentName`; read-only Supervisor fan-out table with per-space allowlist match; Connection test = single probe for Genie or per-space probe matrix (2 s stagger ADR-0003) for Supervisor; Knowledge pack live picker inline. **L8 closed** — proxy refuses to start in production when `PROXY_INLINE_CREDENTIALS_MODE !== "off"`. **L6 mitigated** — dev-mode `[security]` startup banner makes the unauthenticated embed-token route posture visible.
- **Phase 5 — beast-mode four.** System Proxy status (live 10 s `/api/health` poll + latency badge); System Diagnostics (rolling 20-event buffer + last 20 console errors via new `pulseplay:bi-event` window event + monkey-patched `console.error`); System Export bundle (JSON download with token/secret redaction); Advanced Reset section / Reset all / Danger zone gated by type-to-confirm. Retired floating gear popover (now direct navigation to `/settings`). Repointed Pulse Cycle H Display tab to "Open Settings → Preferences" link — single source of truth for display prefs. New modules: [diagnosticsBuffer.ts](../../playground/src/settings/diagnosticsBuffer.ts), [exportBundle.ts](../../playground/src/settings/exportBundle.ts). **MVP 0.2 functional core complete.**
- **Phase 6 medium cleanup — beast-mode five.** Audit closeout. L15 (pack path-traversal regex) + L17 (config schema validator, new [configValidator.js](../../proxy/lib/configValidator.js)) + L14 (probeClient profile-name regex, new `ProbeInvalidProfileError`) + L18 (admin token-cache stats + purge endpoints, new `_adminAuthOk` helper) + L12 (`safeAuthorPrompt` = `redactAuthorPrompt` + `stripInstructionKeywords` for prompt-injection keyword stripping in [pulse/promptRedaction.ts](../../playground/src/pulse/promptRedaction.ts)). Explicitly **ACCEPTED** L9 (CSP origin granularity), L10 (`apiBaseUrl` env override is build-time), L13 (per-user PBI report ACL = Phase 9) with risk-acceptance log in SETTINGS_SPEC § 15.5. **Audit surface now green for pilot.**
- **Phase 8 KB UI — beast-mode six.** New `/knowledge` page separate from Settings. Path-based router (no new dep). KnowledgeShell with header + left rail (installed packs) + content pane with section tabs (Overview / Glossary / Ontology / References / Sub-verticals / Runtime use / Demos). Sub-verticals tab has inner left rail + per-sub-vertical content. Runtime-use tab honestly describes what pack content the AI injects TODAY vs what's available for human review only. Proxy: `loadPackDetail` + `loadSubVerticalDetail` + `isSafePackSegment` in [packRegistry.js](../../proxy/lib/packRegistry.js); new endpoints `GET /assistant/knowledge/packs/:pack` + `GET /assistant/knowledge/packs/:pack/sub-verticals/:subVertical` with allowlist gating + path-traversal regex. Settings › AI › Browse library ↗ deep-link wired. Lives at [playground/src/knowledge/](../../playground/src/knowledge/).
- **Power BI embed-token hardening (P0).** `POST /assistant/embed-token/powerbi` now rejects browser-supplied RLS/effective-identity payloads, derives optional RLS identities only from server-side config or verified IdP claims, gates `Edit` behind `powerBiAllowEdit=true`, and caches by workspace/report/dataset/access/identity hash. Manual Power BI token paste in `EmbedConfigForm` is hidden unless `VITE_PULSEPLAY_ENABLE_MANUAL_PBI_TOKEN=true` outside production; backend-issued mode requests View only. See [docs/HANDOVER.md](../HANDOVER.md) 2026-05-14 entry.
- **Playground viewport controls.** The outer AI/BI shell now supports per-pane maximize/focus, restore, minimize with restore dock, pin/unpin focused startup pane, and open AI/BI in `?focus=ai|bi` pages. The non-focused pane stays mounted during shell focus when both panes are enabled. Browser smoke caught and Codex fixed a duplicate restore-label bug; mounted tests cover minimize dock, Show both, `window.open`, and popstate sync. See [docs/HANDOVER.md](../HANDOVER.md) 2026-05-14 viewport entry.
- **Focused pane chrome overlap closeout.** Rajesh's screenshot first showed the focused-pane controls (`Restore / Minimize / Pin / Page`) overlapping the fixed Pulse `Connected | Managed` pill, and the first fix reserved a right-side collision zone. The newer Center Console slice removes that fixed pill entirely and restores compact focused chrome; regression coverage lives in [viewportControls.integration.test.tsx](../../playground/src/__tests__/viewportControls.integration.test.tsx). See [docs/HANDOVER.md](../HANDOVER.md) 2026-05-14 and 2026-05-16 entries.
- **Production auth hardening (P0).** `proxy/server.js` now has explicit `PROXY_AUTH_MODE` values (`idp`, `shared-key`, `idp-or-shared-key`, `none`), production startup validation for `NODE_ENV=production` / `PROXY_REQUIRE_AUTH=true`, shared-key aliases (`PROXY_SHARED_KEY`, `PROXY_KEY`, `GENIE_PROXY_SHARED_KEY`), IdP/shared-key request enforcement, and audit reasons for rejected auth requests. See [docs/HANDOVER.md](../HANDOVER.md) 2026-05-14 production-auth entry.
- **BIAdapter.getMetadata() — Power BI (commit `c7759bd`).** Added optional `getMetadata?(): Promise<BIMetadata | null>` to the [BIAdapter](../../playground/src/biPanel/BIAdapter.ts) contract. Power BI adapter implements it using `report.getActivePage()` + `page.getVisuals()` + `report.getFilters()` with visual-type/title heuristics for measure/dimension/kind classification, and per-call try/catch for partial degrade (downstream knows what's known vs what failed). [AISidebar](../../playground/src/components/AISidebar.tsx) discovery effect calls `adapter.getMetadata()` before `getDiscoverySnapshot` so the proxy can compute `reachableFrames` honestly. Tableau/Qlik/Looker stay `null` until SDK graduation in v0.3+.
- **BI Live Controls Phase A (commit `f20b00f`, Settings IA fix #6 partial).** Rajesh-driven: Settings page becomes the canonical authoring surface for the Power BI embed config. New dedicated store [embedConfigStore.ts](../../playground/src/settings/embedConfigStore.ts) (`localStorage` key `pulseplay:bi-embed-config`, window event `pulseplay:embed-config-change`, cross-tab `storage` subscription). [BiGroup.tsx](../../playground/src/settings/groups/BiGroup.tsx) Embed leaf renders `<EmbedConfigForm>`; Authentication leaf shows live tokenMode/groupId/report id; Canvas leaf shows tile mode. **3 of 4 PhaseStubs gone.** **Intentionally separate from `settingsStore.tsx`** to avoid merge collision with Codex's open Allowlist lane. Phase B (App.tsx adopts `useEmbedConfig`; Pulse sidebar inline form replaced by status row + deep-link) queued for Codex after Allowlist.
- **PaneChrome visual-weight tightening (commit `e509994`, CSS-only).** Rajesh feedback "the interface is really looking unprofessional now" → pure visual-weight reduction in [App.tsx](../../playground/src/App.tsx) PaneChrome: smaller buttons (fontSize 12→11, minHeight 28→22, padding 0 9px→0 7px), lighter borders, subtle ghost background, softer text. Header padding 7px→5px vertical, gap 10→8, title fontSize 12→11.5 + fontWeight 700→600. Toolbar gap 6→4. Right-side reserve in focused mode `min(228px,50vw)→min(200px,50vw)`. **No behavior changes.** Loosened a brittle exact-string padding assertion in [viewportControls.integration.test.tsx](../../playground/src/__tests__/viewportControls.integration.test.tsx) to a regex matching the clamped-gutter pattern.
- **Support bundle deep redaction (commit `16b5ee3`).** New `redactDeep()` walker in [exportBundle.ts](../../playground/src/settings/exportBundle.ts). Closes three leak paths: nested JSON localStorage values (e.g. `{"config":{"accessToken":"plain-secret"}}`), diagnostic event payloads (vendor `filtersApplied` etc.), and `proxy.health`. Depth cap 8, array cap 200, string cap 4 KB. +7 vitest cases.
- **Allowlist fail-closed P1 (commit `30b2e21`).** [settingsStore.tsx](../../playground/src/settings/settingsStore.tsx) + [App.tsx](../../playground/src/App.tsx) + [BIPanel.tsx](../../playground/src/biPanel/BIPanel.tsx) now distinguish dev-unconfigured (`allowlist?.configured === false`) from governance-fetch-failed (`allowlist === null && allowlistError !== null`). New `isAllowlistFailClosed(state)` helper exported from settingsStore. Reducer `allowlist/error` keeps the prior allowlist on refresh-after-success failure; only first-load failure flips to fail-closed. BIPanel: new `allowlistFailClosed` prop refuses to mount; late-arriving restrictive allowlist destroys an already-mounted adapter and surfaces "Governance update blocked this BI surface". App.tsx error banner differentiates the two states with `role="alert"` vs `role="status"`. +9 tests.
- **BI Live Controls Phase B (commit `923c192`).** App.tsx adopts `useEmbedConfig()` from the dedicated store. Pulse sidebar inline EmbedConfigForm retired in favor of a status row + deep-link to `/settings/bi/embed`. Edits in Settings live-update the playground without refresh, including cross-tab via the storage event. **Behavior change:** embedConfig now persists across reloads. Settings → BI → Embed leaf's "refresh to apply" note removed.
- **PaneChrome Fix #1 + Fix #2 (commit `eb5820b`).** Minimize/Pin/Unpin/Open-in-separate-page collapse into a single `⋮` overflow menu with `role="menuitemcheckbox"` for Pin/Unpin. Maximize/Restore + Both stay inline. New `quiet` prop on PaneChrome hides the toolbar when the pane has nothing to operate on (BI pane wires `quiet={!hasEmbedConfig}`). Integration test gets an `openOverflowFor()` helper + seeds embedConfig in beforeEach so BI chrome is non-quiet for toolbar assertions.
- **GenericIframeAdapter.getMetadata() = null (commit `0ea3ed0`).** Iframe-only adapters (generic-iframe + Tableau/Qlik/Looker stubs that extend it) now have an explicit `async getMetadata(): Promise<BIMetadata | null> { return null }`. TypeScript discoverability + honest contract documenting why these adapters can't introspect.
- **Sidebar rebrand "AI Assistant" → "PulsePlay AI" (commit `7c1bc28`).** Disambiguates the PulsePlay sidebar from the Power BI Copilot that may render inside an embedded report. Title in AISidebar + PaneChrome subtitle updated. Viewport-control aria-labels untouched (they refer to the pane axis, not the product).
- **Databricks-forward option strategy draft (commit `97b6b61`).** Added a discussion draft to [docs/AGENT_SYNC.md](../AGENT_SYNC.md), not canonical architecture yet. Planning frame: Power BI is current-state / transition bridge, Databricks-native assets are the likely destination, and PulsePlay should preserve shift-left and shift-middle optionality through additive adapter evolution rather than a brittle hard pivot.
- **Databricks-forward canonical strategy (2026-05-16).** Added [docs/DATABRICKS_FORWARD_STRATEGY.md](../DATABRICKS_FORWARD_STRATEGY.md) and cross-linked it from Architecture, Roadmap, and Settings Spec. Codex also appended a strategy/feature-map review plus wizard P1 risk scan to [docs/AGENT_SYNC.md](../AGENT_SYNC.md). Key posture: Databricks-native first, Power BI as governed bridge, `BIAdapter` kept as implementation contract while the broader "insight surface" concept evolves additively.
- Updated SETTINGS_SPEC § 15 (all HIGH closed; MEDIUM closed/accepted; LOW L17+L18 closed) + § 16 (Phase 6 ticked); updated HANDOVER 2026-05-13 entries (5 separate entries for the five beast-mode cycles); updated AGENDA phase markers; cross-referenced from feature_settings_spec.md.

**Validation:**

- `proxy`: `node --check server.js`, `node --check lib/allowlist.js`, `node --check lib/packRegistry.js`.
- `proxy`: focused `npm test -- allowlist packRegistry server` passed.
- `proxy`: full `npm test` passed, 428/428.
- `playground`: `npm run lint` (tsc --noEmit) passed.
- `playground`: full `npm test` passed after Phase 8 KB UI — **257/257** (+17 new this cycle: 12 knowledgeRoute + 5 KnowledgeShell).
- `playground`: `npm run build` green.
- `proxy`: full `npm test` passed **464/464** (+7 new this cycle: packRegistry.detail).
- `proxy`: after Power BI hardening, full `npm test` passed **630/630**.
- `playground`: after Power BI hardening, `npm run lint` passed, full `npm test` passed **338/338**, and `npm run build` passed.
- `playground`: after viewport controls and warning cleanup, `npm.cmd test -- viewportControls` passed **16/16**, full `npm.cmd test` passed **354/354**, `npm.cmd run lint` passed, and `npm.cmd run build` passed.
- `playground`: after focused-pane chrome overlap closeout, `npm.cmd test -- viewportControls.integration --silent` passed **15/15**, `npm.cmd run lint` passed, full `npm.cmd test -- --silent` passed **388/388**, and `npm.cmd run build` passed.
- `proxy`: after final cross-validation, full `npm.cmd test` still passed **630/630**.
- `proxy`: after production auth hardening, `node --check proxy/server.js` passed, focused `npm.cmd test -- productionAuth` passed **16/16**, focused `npm.cmd test -- server --runInBand` passed **119/119**, and full `npm.cmd test` passed **646/646**.
- `playground`: after BIAdapter.getMetadata + AISidebar discovery wiring, full `npx vitest run --silent` passed **388/388**.
- `playground`: after BI Live Controls Phase A (15 new embedConfigStore tests), full `npx vitest run --silent` passed **403/403**.
- `playground`: after PaneChrome polish + test-assertion loosening, focused `npx vitest run src/__tests__/viewportControls.integration.test.tsx` passed **15/15**, full `npx vitest run --silent` passed **403/403**, `npx tsc --noEmit` clean.
- `playground`: after Support bundle deep redaction, full `npx vitest run --silent` passed **412/412** (+7 new redaction cases).
- `playground`: after Allowlist fail-closed P1, full `npx vitest run --silent` passed **421/421** (+9 new: 6 settingsStore + 3 BIPanel.failClosed).
- `playground`: after PaneChrome Fix #1 + #2, full `npx vitest run --silent` still passed **421/421** (test-helper updates only, no new assertions).
- `playground`: after GenericIframeAdapter.getMetadata + sidebar rebrand, full `npx vitest run --silent` passed **423/423** (+2 new getMetadata cases). `npx tsc --noEmit` clean.
- `playground`: after Databricks-forward strategy docs, `npm.cmd run lint` passed clean. No code changed.
- `playground`: after setup/settings relationship audit + control-depth polish, `git diff --check` clean, `npm.cmd run lint` clean, focused `FirstRunWizard` tests **30/30**, full `npm.cmd test -- --silent` passed **467/467**, `npm.cmd run build` passed, and a live Vite root smoke returned `http://127.0.0.1:5173/` successfully.
- `playground`: after common AI context model + setup grouping, `git diff --check` clean, focused `setupStep5DomainPresets` tests **3/3**, `npm.cmd run lint` clean, full `npm.cmd test -- --silent` passed **470/470**, and `npm.cmd run build` passed.
- `docs`: after Chat visualization knowledge base add-on, validation is doc-only (`git diff --check`).
- `docs`: after Knowledge Base source governance pass, validation is doc-only (`git diff --check`).
- `playground`: after wizard repeat-ask + zero-vendor guard + copy-link polish, focused `npm.cmd test -- FirstRunWizard AISidebar leafScrollAndChips --silent` passed **73/73**, `npm.cmd run lint` clean, full `npm.cmd test -- --silent` passed **494/494**, `npm.cmd run build` passed, and `git diff --check` passed with only LF-to-CRLF working-copy warnings.
- `playground`: after Setup pill + Settings setup tree, `npm.cmd run lint` passed, focused `npm.cmd test -- settingsRoute SettingsShell leafLabels leafScrollAndChips setupReadiness viewportControls --silent` passed **55/55**, full `npm.cmd test -- --silent` passed **502/502**, `npm.cmd run build` passed, and `git diff --check` passed.
- `playground`: after Settings/Console IA consolidation, focused `npm.cmd test -- AiGroup leafLabels viewportControls PulseShell --silent` passed **40/40**, `npm.cmd run lint` passed, full `npm.cmd test -- --silent` passed **496/496**, `npm.cmd run build` passed, `git diff --check` passed with only LF-to-CRLF warnings, and the Vite root returned HTTP **200**.

**Current limitations / brutal honesty:**

- PulsePlay still does not have a full enterprise knowledge-base runtime. It has pack content, pack discovery, a read-only Knowledge Base browser, probe/matcher inference, prompt-context injection, and Databricks Vector Search route scaffolding.
- Missing runtime pieces: retrieval provider adapters, ACL-trimmed retrieval, citations, retrieval profiles, `GroundingBundle` in AISidebar, and KB evals.
- **Databricks P2-P8 is enablement-complete, not production-complete.** Live discovery and first surfaces exist; production hardening still needs approved Databricks App resources, allowed embedding surfaces, SP client-secret config for AI/BI external embed, a real Vector Search index, evidence/lineage expansion, and live app deployment smoke.
- **Audit surface green for pilot.** All 8 HIGH closed/mitigated; all 7 MEDIUM closed (L11/L12/L14/L15) or ACCEPTED with explicit re-open trigger (L9/L10/L13); L17 + L18 LOW closed. Remaining open LOWs (L16 + L19) deferred to Phase 9.
- **MVP 0.2 functional + security core is complete after Phase 6.** All 5 Settings groups wired live; legacy gear popover + Pulse Cycle H Display tab retired in favor of `/settings` as the canonical surface; System health + diagnostics + export bundle functional; Advanced reset actions gated by type-to-confirm; security gates landed at every defense-in-depth layer.
- **Phase 8 KB UI shipped** as a separate `/knowledge` page (read-only browser for installed pack content). Settings deep-links here via AI › Browse library ↗.
- **Power BI embed-token P0 is closed in code and tests**, but a live credentialed smoke is still needed against an org Power BI report to verify the chosen `powerBiRlsUsernameClaim` aligns with that report's dataset RLS model.
- **Playground viewport controls are closed in code and tests.** Remaining UX polish is field feedback only; no known code/test gap remains for maximize/minimize/restore/pin/open-page.
- **Focused pane status-pill overlap is retired in code and tests.** The old Pulse fixed top-right status pill is gone; connection/scope status lives inside Console. A new app-owned setup/readiness pill lives in the top bar and opens Settings › Setup, avoiding the old overlap path.
- **Production auth P0 is closed in code and tests.** Deploy `PROXY_AUTH_MODE=idp` if every browser request must carry verified end-user identity; `idp-or-shared-key` intentionally permits controlled service-to-service shared-key fallback. Live enterprise JWKS verification still needs environment smoke.
- **Phase 9 framing tightened (do not collapse again):** what was previously listed as "Phase 9 Vendor expansion" is actually two unrelated things:
  - **Phase 9a — Configuration expansion** (✅ AVAILABLE TODAY, pure config in `proxy/config.json`, no code). Adding more Genie spaces / Supervisor fan-outs / Azure OpenAI / Bedrock / Mosaic Foundation Model profiles / Power BI workspaces / AAD tenants / packs is a config edit. The proxy already has routes for every connector type in `config.example.json`. See [DEPLOY_MVP_0.2.md](../DEPLOY_MVP_0.2.md) for the literal deployer checklist.
  - **Phase 9b — Stub-to-SDK graduation** (⏳ v0.3+, per-vendor code). Only Tableau / Qlik / Looker need real adapter code beyond the iframe stub today. Triggered by an org standardising on a non-PBI BI tool. Power BI is already fully wired with `powerbi-client` SDK.
- **Phase 10 — Fabric feature support** (⏳ v0.4+, additive code in PBI adapter). Direct Lake / Dataflow Gen2 / semantic-link API surfaces need new code paths inside the existing Power BI adapter. Triggered by an org enabling Fabric. Classic PBI (Import / DirectQuery / Composite) already works plug-and-play today.
- **Remaining gate before pilot:** live credentialed Power BI + Genie/Supervisor smoke against a deployed proxy with a populated allowlist. No further code work required for the MVP 0.2 cell.
- App.tsx still holds its own copies of the persisted `pulseplay:*` keys alongside the new store (intentional Phase 2 coexistence). Phase 5 retires the duplicates. The `pulseplay:display-change` event keeps them synced.
- `AGENTS.md` is untracked and was not edited by this session.

**Next recommended implementation cycle:**

1. **Databricks App deployment smoke.** Bind real App resources, confirm `APP_RESOURCE_*` values land in `/health`, and verify `/launchpad` from the deployed Databricks App origin.
2. **AI/BI external embed hardening.** Configure service-principal OAuth values, install/approve `@databricks/aibi-client` if allowed, and smoke one published dashboard through the scoped-token path.
3. **Genie embed pilot.** Enable the Databricks Embed Genie preview, configure allowed embedding surfaces, paste one generated iframe/src into Settings, and confirm the `databricks-genie` adapter renders with clipboard permission.
4. **Evidence Drawer expansion.** Add UC metric-view details, dashboard dataset SQL, Genie conversation evidence, Vector Search citations, request IDs, and lineaged source metadata.
5. **Vector Search retrieval runtime.** Once an approved index exists, wire the retriever contract, ACL negative tests, source chunk/citation mapping, and eval fixture.
6. **KB source governance execution.** Claude should review source tiers and linter IDs, then choose the first execution slice: finish converting `cpg-fmcg/references.md`, add audit headers to all Markdown KB modules, or write a docs-only pack-linter spec.
7. **Chat visualization rule storage shape.** Decide whether [CHAT_VISUALIZATION_KNOWLEDGE_BASE.md](../CHAT_VISUALIZATION_KNOWLEDGE_BASE.md) becomes static `chartKnowledgeRules.ts`, PulsePack YAML, or `DomainContextProfile.visualizationGuidance`.
8. **StructuredAuthoringEditor first slice.** Pick Settings AI guidance or Prompt IR authoring first. Implement required sections, parameter insert controls, validation, and compiled middleware preview per [STRUCTURED_AUTHORING_STANDARD.md](../STRUCTURED_AUTHORING_STANDARD.md).
9. **Frame-to-prompt translator specialization.** Frontend and proxy already bridge `body.frame`; the next step is making Prompt IR translators specialize output by selected frame.
10. **Live credentialed smoke** against an org Power BI report + Genie/Supervisor profile + enterprise IdP JWKS.
11. **Phase 11b dispatcher migration** — wire `proxy/lib/promptDispatcher.js` into the live request paths (currently shipped as additive, byte-identical to legacy for Genie). Requires careful byte-identity regression coverage.
12. **Tableau / Qlik / Looker SDK graduation** (v0.3+) — replace iframe stubs with real SDK adapters only when an org standardizes on one of those tools.
