{
    "name": "pulseplay-proxy",
    "version": "1.1.0",
    "description": "PulsePlay AI Proxy — connector-agnostic backbone routing to multiple AI brains (Genie / Supervisor / Azure OpenAI / Bedrock / Foundation Model / future MCP) for any BI surface. Inner-source v1.",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch server.js",
        "test": "jest"
    },
    "dependencies": {
        "express": "^4.21.2"
    },
    "engines": {
        "node": ">=18"
    },
    "devDependencies": {
        "jest": "^30.3.0",
        "supertest": "^7.2.2"
    }
}
