# Databricks Apps Deployment

This proxy-only README is superseded by the single PulsePlay installation guide:

- [docs/DEPLOY_DATABRICKS_APP.md](../docs/DEPLOY_DATABRICKS_APP.md)

The old notes in this file described an inherited proxy-only app named
`test-superuser-genie-powerbi`. PulsePlay now deploys the combined React shell
and Node proxy from the repo root [../app.yaml](../app.yaml), so deployers should
not follow the older proxy-only workflow or hardcoded Genie-space examples.

Keep this file as a signpost only; the deployer runbook lives in the docs guide.
