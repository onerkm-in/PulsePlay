node_modules/
dist/
out/
build/

# Local PulsePlayData/ generated by smoke runs or local launcher invocations.
# Authors who run the launcher in this enabler directory get a sibling
# PulsePlayData/ folder; never commit it.
PulsePlayData/

# pkg/SEA build artifacts
PulsePlay.exe
*.exe
sea-prep.blob
sea.blob

# Logs that may land in this directory during local debug sessions
*.log
