// The visual source is GENERATED from playground/src/pulse (already linted in
// PulsePlay). Re-linting the generated tree under the PBI config is redundant
// and would just flag PulsePlay-idiomatic code. Lint nothing here; correctness
// is enforced by `npm run typecheck` + PulsePlay's own CI.
export default [
  { ignores: ["src/**", "style/**", ".tmp/**", "dist/**", "node_modules/**"] },
];
