# Pulse PBI Snapshot — Provenance

This document records the exact origin of the files in this folder so future maintainers can audit drift, refresh the snapshot, or correlate Pulse PBI commits with PulsePlay history.

## Source

| Field | Value |
|---|---|
| Upstream repo | `https://github.com/onerkm-in/pbi-genie-visual` |
| Upstream branch | `main` |
| Upstream commit SHA | `9e3b7b6fffdbea8d4ca3390a7ae5eaeb8307ccce` |
| Upstream commit subject | `Consolidate Power BI workspace examples` |
| Local sibling path at import time | `D:\Working_Folder\Projects\PBIGenieVisual` |
| Import date (UTC) | 2026-05-21 |
| Imported during | PB0 cycle |

## Import method

**Snapshot (file copy) + provenance doc.** Rejected `git subtree add` after inspection:

- The upstream repo had only **two commits** in its history at import time (`9e3b7b6` and `bb6d07c`). `git subtree` preserves source history into the target repo; with two commits the preservation gain is symbolic.
- Snapshot keeps PulsePlay's own commit history clean (no rewritten upstream commit messages threading through `git log`).
- `docs/PULSE_SYNC.md` is the sync mechanism going forward, not bidirectional subtree pulls.
- ADR-0010 explicitly warns against submodule-only final state because source ZIP downloads silently omit submodule content. A snapshot lives directly in the tree and ZIP-downloads cleanly.

Trade-off accepted: future refreshes from upstream require either a fresh re-snapshot (update this doc with the new commit hash) or hand-applied patches with a clear PR description. The repo is small enough (~672 KB of tracked source) that re-snapshot is cheap.

## What was copied

Everything in `../PBIGenieVisual` EXCEPT the exclusions below was copied into `enablers/pulse-pbi/`:

| Path | Status | Notes |
|---|---|---|
| `src/` | included | The custom visual source code. |
| `proxy/` | included | A historical reference proxy in the upstream repo. **Not the production proxy** — that's `proxy/` at the PulsePlay repo root. Kept here for historical reference only. |
| `style/` | included | Pulse PBI's `gn-*` namespaced styles. |
| `assets/` | included | Visual icons + images packaged into `.pbiviz`. |
| `docs/` | included | Upstream documentation; preserves original context. |
| `examples/` | included | Sample Power BI reports + queries. Useful for testing/demo. |
| `package.json` | included | Pulse PBI's npm dependency manifest. |
| `package-lock.json` | included | For reproducible builds. |
| `pbiviz.json` | included | Power BI custom visual manifest. |
| `capabilities.json` | included | Power BI capability declaration. |
| `eslint.config.mjs` | included | Pulse PBI's lint config. |
| `playwright.config.ts` | included | Pulse PBI's e2e test config. |
| `chat.spec.ts` | included | Top-level Playwright E2E spec. Requires `pbiviz start` running; not part of the vitest unit run. |
| `tsconfig.json` | included | Required by `pbiviz package`. **This file was missed in the initial copy pass; the PB0d build attempt caught the gap and it was copied in before commit.** |
| `README.md` | renamed to `README.upstream.md` | Original upstream README, kept verbatim. The new `README.md` at this folder documents PulsePlay-side context. |

## What was excluded

| Path | Reason |
|---|---|
| `node_modules/` | 63 MB of installed deps; reproduce via `npm install`. PulsePlay's repo-root `.gitignore` already excludes node_modules everywhere. |
| `dist/` | Build output; reproduce via `pbiviz package`. PulsePlay's repo-root `.gitignore` already excludes dist everywhere. |
| `webpack.statistics.prod.html` | 168 KB build-statistics artifact. Regenerated by builds; not source. |
| `.git/` | Upstream repo's own git directory. PulsePlay tracks the import through THIS doc, not the upstream `.git`. |

A local `enablers/pulse-pbi/.gitignore` reinforces the exclusion of `node_modules/` and `dist/` in case PulsePlay's repo-root `.gitignore` patterns ever diverge.

## How to refresh from upstream

When the upstream `pbi-genie-visual` repo gains commits worth pulling in:

1. Verify your local `../PBIGenieVisual` is on a clean commit you trust.
2. Delete the contents of `enablers/pulse-pbi/` EXCEPT `README.md` and this file.
3. Re-copy from upstream using the same file list above.
4. Update **the Source table at the top of this file** with the new commit hash + date.
5. Update [`docs/PULSE_SYNC.md`](../../docs/PULSE_SYNC.md) changelog with the refresh entry.
6. Run a focused build verification — `cd enablers/pulse-pbi && npm install && npx pbiviz package` — and note any breakage in the PR.
7. Land the refresh in a single commit titled `chore(pulse-pbi): refresh snapshot to <short-sha>`.

The refresh is intentionally manual + auditable. Automated upstream tracking would hide drift in the lockfile, in dependency versions, or in policy that has cross-impact on PulsePlay's proxy contract.

## Verification

After import, the following invariants should hold:

- `enablers/pulse-pbi/pbiviz.json` exists and parses as valid JSON.
- `enablers/pulse-pbi/capabilities.json` exists and parses as valid JSON.
- `enablers/pulse-pbi/tsconfig.json` exists with `compilerOptions.outDir` set (required by `pbiviz package`).
- `enablers/pulse-pbi/src/visual.tsx` exists (the custom visual entry point).
- `enablers/pulse-pbi/src/__tests__/` directory exists (Pulse PBI's own test suite).
- `enablers/pulse-pbi/node_modules/` does NOT exist in the tracked tree.
- `enablers/pulse-pbi/dist/` does NOT exist in the tracked tree.
- `enablers/pulse-pbi/.tmp/` does NOT exist in the tracked tree.

## PB0d build verification (2026-05-21)

The first end-to-end build from this location ran during PB0d and succeeded:

| Step | Result |
|---|---|
| `npm install --no-audit --no-fund --prefer-offline` | 242 packages installed in 11s |
| `npm run lint` (eslint) | pass, no output |
| `npm test` (vitest) | 87/87 unit tests pass. `chat.spec.ts` fails to LOAD under vitest (it's a Playwright E2E spec; dual-runner config issue inherited from upstream, not caused by the snapshot). |
| `npx pbiviz package` (after copying the missed `tsconfig.json`) | `done Build completed successfully` → `dist/PBIGenieVisual87799D3556EA4890BCBE3FF9F9A095F5.2.1.0.0.pbiviz` produced |
| Build artifacts properly gitignored | `git status --ignored` confirms `dist/`, `node_modules/`, `.tmp/`, `webpack.statistics.prod.html` all excluded from the tracked tree |

The PB0d attempt is what surfaced the missing `tsconfig.json`. Without trying the build, the snapshot would have been shipped broken and the first downstream user would have seen the same `Cannot read properties of undefined (reading 'outDir')` error from `pbiviz`. **Cost of running PB0d: ~15 minutes. Value: caught a real defect before merge.**

## 2026-05-21 post-import rename — PBIGenieVisual → PulseVisuals

The visual identity in `pbiviz.json` and the npm package name in `package.json` were renamed from the upstream `PBIGenieVisual` / `pbi-genie-visual` family to `PulseVisuals` / `pulse-visuals` to align with the **Pulse** brand family used across the PulsePlay ecosystem (PulsePlay web app + Pulse PBI custom visual + future Pulse desktop). Rationale: the upstream "GenieVisual" naming was project-local and predates the ecosystem rename to Pulse; the artifact a user actually installs in Power BI Desktop should reflect the family name rather than the heritage project name.

| File | Field | Before | After |
|---|---|---|---|
| `pbiviz.json` | `visual.name` | `PBIGenieVisual` | `PulseVisuals` |
| `pbiviz.json` | `visual.displayName` | `PBI Genie Visual` | `Pulse Visuals` |
| `pbiviz.json` | `visual.guid` | `PBIGenieVisual87799D3556EA4890BCBE3FF9F9A095F5` | `PulseVisuals87799D3556EA4890BCBE3FF9F9A095F5` |
| `pbiviz.json` | `author.name` | `PBIGenieVisual` | `Pulse` |
| `package.json` | `name` | `pbi-genie-visual` | `pulse-visuals` |
| `chat.spec.ts` | `test.describe` label | `PBIGenieVisual E2E` | `PulseVisuals E2E` |

**What did NOT change**, and why:

- **`README.upstream.md`** — kept verbatim by design. It's the original upstream README at import time; rewriting it would lose the historical truth.
- **`enablers/pulse-pbi/proxy/`** — historical reference proxy, ADR-0010 / PB0 tripwire says do not modify.
- **`examples/SampleSuperStoreAnalysis/`** — sample .pbix-derived demo data containing the OLD visual GUID. Power BI Desktop extracted those folder names from the .pbix at the time the sample was authored; they are frozen historical artifacts, not the active visual identity. Re-saving a new .pbix using the renamed visual would produce fresh `PulseVisuals87799...` folders, but the existing committed demos remain valid as historical reference.
- **Upstream URLs above** (`https://github.com/onerkm-in/pbi-genie-visual`, `Local sibling path at import time: ../PBIGenieVisual`) — factual references to the upstream repo and import-time local sibling. They must remain accurate so the refresh procedure below still names what was actually pulled in.
- **The 32-char hex portion of the GUID (`87799D3556EA4890BCBE3FF9F9A095F5`)** — preserved deliberately. Only the human-readable prefix changed. Internal Pulse PBI deployments that installed the upstream version will see this as a NEW visual identity (because pbiviz concatenates name + hex into a single GUID string at build time) — that is acceptable for this inner-source phase and noted here so the next deployer doesn't expect in-place version updates.

**Rebuild verification (post-rename, 2026-05-21):**

| Step | Result |
|---|---|
| `npm install --no-audit --no-fund --prefer-offline` (regenerates `package-lock.json` with new package name `pulse-visuals`) | `up to date in 2s` (no new packages — name change is metadata-only) |
| `npm run lint` (eslint) | pass, no output |
| `npm test` (vitest) | 87/87 unit tests pass. `chat.spec.ts` still fails to LOAD under vitest for the same dual-runner reason PB0d documented (it's a Playwright spec); this is upstream-inherited config drift, not caused by the rename. |
| `npx pbiviz package` | `done Build completed successfully` → `dist/PulseVisuals87799D3556EA4890BCBE3FF9F9A095F5.2.1.0.0.pbiviz` produced (~106 KB). The pre-rename `PBIGenieVisual87799...pbiviz` from PB0d is still present in `dist/` from the earlier build; both are gitignored and not tracked. |
| Build artifacts still gitignored | confirmed — `dist/`, `node_modules/`, `.tmp/`, `webpack.statistics.prod.html` all excluded |
