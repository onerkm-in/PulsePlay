## Summary

-

## Validation

-

## Pulse PBI sibling sync

Pulse PBI copy-port: N/A

Use `queued` or `done` instead of `N/A` when this PR changes copy-portable modules listed in `docs/PULSE_SYNC.md`; add the `sibling-sync` label and update the ledger.

## Ecosystem cascade

Pulse PBI impact: N/A

Desktop EXE impact: N/A

Use `queued`, `done`, `automatic via proxy`, or `future DX consideration` when the PR changes shared proxy contracts, portable modules, result envelopes, governance/audit behavior, Settings/first-run UX, local persistence, or browser/runtime assumptions. Update `docs/PULSE_SYNC.md` when the answer is not `N/A`.
